# 工作记录 · 2026-09-06（项目文件按用途整理）

> 纯整理会话，无技术产出。文件位置有变化，引用路径以本次整理后为准。

## 做了什么

把根目录散落的文件按用途归位，沿用既有分类（设计/归档/工作日志/参考/学习库），新增三个目录：

| 新目录 | 收纳内容 | 来源 |
|---|---|---|
| `数据与训练/` | `train/`（实拍原始数据）、`build_real_data.py`、`real_data/`、`real_data.zip`、`jetson/` 更名 **`v2训练流水线/`**（AutoDL 指南 + run_v2_training.sh + v1 合成包） | 9-5 上半场数据线产出 |
| `工具/飞控调试/` | check_fc / check_gps / verify_params / activate_lazy_params / read_params 五个脚本，新增 README（用法与坑：断开 MP、参数导入两遍、COM 号会变） | 9-5 晚间飞控线产出 |
| `手册/` | Mission Planner 任务规划文档（中/英）、Pixhawk V6X 使用手册 | 9-2 参考资料 |

另：3 个 `param_backup_*.param` 移入 `设计/参数备份/`（**232322 为已配置基线**，230343 刷机默认，230057 中间态）；`设计/README.md` 补上 firmware/ 与 参数备份/ 索引。

## 清理

- `train/copy/`（52 张）+ `copy.zip`：与 `train/images/` 逐字节比对**全部重复**（9-5 分析时的临时副本）→ 移入 `归档/train工作副本_20260905/`
- 根目录 `__pycache__/`：只有一个 pyc 缓存 → 删除

## 同步修正（防引用失效）

- `build_real_data.py`：SRC/DST 从写死盘符路径改为**跟随脚本所在目录**（`Path(__file__).parent`），以后整个 `数据与训练/` 挪到哪里都能跑。
- `设计/总体方案与执行计划.md`（SSOT）文档地图及 §8 的 6 处路径引用同步更新为新位置。
- 新建根 `README.md`：全项目按用途分类的总索引 + 快速上手指引。

## 整理后根目录（同日两次调整后现状）

`01_设计` / `02_数据与训练` / `03_工具` / `04_手册` / `05_硬件资料_Jetson` / `06_比赛原件` / `07_大赛附件1-13` / `08_参考` / `09_工作日志` / `99_归档`，索引见根 `README.md`。

## 追加：文件夹统一重命名（按用途编号）

| 原名 | 新名 |
|---|---|
| 设计 | `01_设计` |
| 数据与训练 | `02_数据与训练` |
| 工具 | `03_工具` |
| 手册 | `04_手册` |
| Jetson Orin Nano CLB套件客户资料 | `05_硬件资料_Jetson` |
| 原件 | `06_比赛原件` |
| 附件1-13(1) | `07_大赛附件1-13`（顺带拍平内部 `附件1-13` 双层嵌套） |
| 参考 | `08_参考` |
| 无人机竞赛学习 | `09_竞赛学习` |
| 工作日志 | `10_工作日志` |
| 归档 | `99_归档` |

编号约定：01~03 干活用的产出与工具，04~09 查阅资料，10 记录，99 归档。SSOT、`01_设计/README.md`、`03_工具/飞控调试/README.md`、根 README 中的路径引用已全部同步更新；归档文档与历史日志按约定不改写。

插曲：`设计` 目录一度无法改名/删除——**Typora 正开着 `总体方案与执行计划.md`** 锁住了目录句柄（排查进程命令行才定位）。已用"新建 `01_设计` → 移入全部内容"等效完成改名，文件本身（含 Typora 正打开的 SSOT）移动成功，Typora 重新保存/打开时请用新路径 `01_设计/总体方案与执行计划.md`。空的旧 `设计` 外壳等 Typora 释放句柄后删除即可。

## 再追加：人工微调与索引同步

用户随后手动调整了三处，相关索引已全部同步：

1. `10_工作日志` → **`09_工作日志`**（工作日志改编号 09，接在资料类之后）。
2. **`09_竞赛学习`（学习知识库）移出本项目**——技能树/学习日志等已不在工作区内。
3. **`08_参考` 拍平**：`2026CUADC_HIT/` 这层目录壳去掉，仓库内容（技术全解、视觉标定、sim、Re0 教程、日志.md 等）直接置于 `08_参考/` 下，`.git` 仍在、仓库功能完好。

同步更新：根 `README.md`（目录表、编号约定 01~03 干活 / 04~08 资料 / 09 记录 / 99 归档、快速上手）、SSOT 文档地图（08_参考 行改写并吸收新人教程指引；删除学习库行；§7 两处 `2026CUADC_HIT/` 路径去掉壳层）。`09_工作日志/2026-09-05.md` 与 `99_归档/` 中的旧路径属历史记录，按约定不改写。

## 下一步（承接 9-5 待办，不变）

- [ ] P0.1：`real_data.zip` + `run_v2_training.sh` + 合成包传 AutoDL 跑 v2 微调（现位于 `02_数据与训练/` 与 `02_数据与训练/v2训练流水线/`）
- [ ] P0.2~0.4：TensorRT 导出实测 / 黑帧预热 / 视觉时间戳补偿
- [ ] GPS 室外验收（3D Fix + ≥8 星 + HDOP<1.5）+ 罗盘校准

## 追加：新建 `10_机载代码/`（任务状态机 M1 骨架）

按 SSOT §5 落地第一版自研代码（参考公开版重写，未复制；本机未编译，待上 Jetson 首次 colcon build）：

- 新目录 `10_机载代码/cuadc_mission/`：ROS 2 包（状态机 M1 骨架）
  - `src/mission_node.cpp`：SSOT §5.1 全状态枚举 + tick(20Hz) 骨架；准备五连 / `LOCK_FRAME` 独立态（2s 内航向变 ≤2°）/ CommandTOL 起飞（0.9×高切 setpoint）/ 弓字形预设航线（smoothstep 时间插值）/ 侦察 6 航点拍照握手 / 返航 / 降落确认（≤0.30m+速度门限+1.5s）/ 独立 DISARM 自动上锁全实现；SEARCH 正式模式与 ALIGN/RELEASE **fail-closed**（M2/M3 接入点已留 `TODO(M2)/TODO(M3)/TODO(M4)` 标记）
  - `config/mission_params.yaml`：30 项参数（默认值 = SSOT §5.4 基线），已核对代码声明 = 代码加载 = YAML 三方一致
  - `launch/mission.launch.py`（支持 `use_sim_time`）+ `package.xml` + `CMakeLists.txt`
- `10_机载代码/README.md`：编译 / SITL 联跑顺序 / M1 验收清单（含接管保护与断流注入测试项）/ 里程碑 TODO 地图
- 根 `README.md` 目录表与编号约定同步（09 记录之后新增 10 自研代码）
- 未动 SSOT；代码内方案注释一律指回 SSOT §5，`m1_no_vision_mode=true` 仅为 M1 演练，M2 后必须关闭

下一步：代码线待 M1 在 SITL 过全流程（含切 GUIDED 接管、断 odom 注入两项保护验证）；数据线 P0.1 v2 微调不变。

## 再追加：接口契约 / 假感知 / P0.4 时间同步 / SITL 脚本（"三件全开"）

- **`接口契约.md`（v1.0）**：检测 PoseArray 字段复用（orientation.x=直径 / y=置信度 / z=契约版本哨兵）、frame_id 哨兵 `cuadc_body_flu`、**stamp=取帧时刻**、心跳 fail-closed（空帧照发，停发=故障=禁投）、侦察握手、契约变更规则（改接口必 bump 哨兵并同步校验脚本）
- **`cuadc_perception/`（ament_python）**：`fake_perception_node`（30Hz 三模拟筒 25/20/15cm + 可注入延迟/噪声，M1~M2 联调用）+ `check_vision_contract`（在线体检：频率/心跳/哨兵/置信度/直径边界/单帧上限/stamp 容差 + <2cm 未去重告警，退出码可接 CI）+ launch/config；真感知 M2 在本包实现
- **P0.4 时间同步**：`时间同步设计.md` + `mission_node.cpp` 增量——odom 历史缓冲（时间窗 3s + 600 条硬上限双裁剪）、`interpolate_odom()` 夹逼插值（太旧/未来戳/断流三种拒绝，绝不外推）、`body_to_local_at()`、**消息 stamp 统一换节点时钟类型**（防 use_sim_time 下 rclcpp::Time 相减抛异常）；参数 30→34（perception_max_delay_s / odom_future_tol_s / odom_interp_max_gap_s / odom_history_span_s），三方核对通过；仿真验收（注入 200ms 延迟、筒位滞后 <5cm）留 M2 联调执行
- **SITL 脚本**：`scripts/setup_env_ubuntu22.sh`（按 HIT SETUP.md 自动化：ROS2 Humble / Gazebo Harmonic / MAVROS+geographiclib / ArduPilot SITL / ardupilot_gazebo 插件 / 工作空间布置+场景生成+colcon build；幂等可重跑，`--skip-heavy` 跳过编译）+ `scripts/run_sitl.sh`（四件套一键：gz sim → sim_vehicle(gazebo-iris JSON→14550) → mavros(fcu_url `udp://:14550@`，轮询等 connected) → 任务节点；日志 /tmp/cuadc_sitl_logs，Ctrl+C 全清理；**默认 use_sim_time:=false**——HIT 包默认 bridge 无 /clock）
- 核查：两脚本 `bash -n` 通过；Python 节点 `py_compile` 通过；README 目录表同步。**本机 Windows 仍无法编译 C++/运行 SITL**，首次实机跑按报错修。

## 三追加：代码上 GitHub（Tracysrs/CUADC）

- `10_机载代码/` 初始化为 git 仓库（main 分支），加 `.gitignore`（build/install/log/缓存）与 `.gitattributes`（**全仓 LF**，保 .sh/.py 上 Jetson 不出换行符事故），首次提交 `M1 骨架：任务状态机 + 感知包 + 接口契约 + P0.4时间同步 + SITL脚本`
- 推送到 **https://github.com/Tracysrs/CUADC**（Private）：远端原有一个 GitHub 自动生成的 README 占位提交，已 rebase 接到其后（README 冲突以我们的真实文档为准），推送后核验远端 main = Initial commit → M1 骨架提交
- 以后代码改动在 `10_机载代码/` 内 commit + push 即可；比赛规则 PDF、HIT 参考仓等不进此库

## 四追加：全项目 GitHub 式管理（结构不动，补齐管理要素）

- **目录 README 全覆盖**：02/03/04/05/06/07/09/99 八个一级目录补 README（清单/用法/注意，按实际扫描结果写），01 与 10 原有；新约定入根 README——"每个目录必须有 README，移动/重命名目录必须同步根 README 与 SSOT 文档地图"
- **根 README 门面化**：新增项目状态速览（M1~M6 里程碑表）、目录树（★ 标注干活产出区）、版本管理说明、关键文档直达表
- **根仓库建立**（commit `b1af69d`，27 文件）：白名单式 `.gitignore` 入库 01_设计 / 03_工具 / 09_工作日志 / 02 的 README+构建脚本+训练指南与脚本；**不入库**：04/05/06/07（第三方版权件与厂商资料）、02 数据集（约 1.3GB）、08_参考（他人仓库）、99_归档（只读）、10_机载代码（已独立成库）
- **插曲（教训）**：首版 .gitignore 写了行内 `#` 注释，gitignore 不支持行内注释导致 `/*` 规则失效，`git add` 开始吞 17GB 厂商资料——及时终止、清残留 index.lock、修正后重新 add，核对入库清单 27 个文件无误才提交。**教训：改 .gitignore 后必须 `git status --short` 核对清单再提交**
- 根仓库暂在本地；如需推 GitHub 建议新建独立私有库（如 `CUADC-project`），与代码库分开

## 五追加：Jetson 实机联通日（免密 SSH + 链路验证 + 工作区部署 + 真机冒烟）

- **免密 SSH 建成**：Windows 生成 ed25519 密钥装入 Jetson（绕开 MobaXterm 括号粘贴坑的 install_key 脚本方案）
- **丢包根治**：USB RNDIS（192.168.55.1）实测严重阵发丢包（SSH 频繁超时，ICMP 60%+ 丢）。探测发现 Jetson WiFi 已在 Zpets 局域网（10.66.24.217，与电脑同段）→ **SSH 全部改走局域网**，USB 链路降级为应急备份通道
- **FC↔Jetson 链路**：TELEM2→40-pin UART 方案实测两串口五波特率全 0 字节（飞控端 SERIAL2 配置正确：MAVLink2 @921600 无流控，问题在物理接线，待查）→ **改用 USB 直连**：CUAV-V6X-v2 枚举 ttyACM0/1，MAVLink2 帧流 + pymavlink 心跳解码通过（QUADROTOR / ARDUPILOTMEGA / STANDBY）。台架阶段用 USB，上机前再换 UART
- **Jetson 环境盘点（远超预期）**：JetPack 6.x（L4T R36.4.7）+ ROS 2 Humble + MAVROS 2.14.0 + GeographicLib 数据集**全部已装**；缺的 pymavlink/pyserial 用清华镜像 `pip --user` 补齐（PyPI 直连会 hang；已留 `pip config set` 命令建议）
- **工作区部署**：两包 tar 流传输到 `~/cuadc_ws/src` → colcon build 通过。坑：user-site setuptools 80.9 与系统 packaging 21.3 不兼容（`canonicalize_version` 报错）→ `pip --user -U packaging` 解决；编译期警告 colcon 需要 setuptools<80，暂不碍事
- **真机冒烟四项全过**：① MAVROS 连 `/dev/ttyACM0` → `connected: true`（STABILIZE）；② mission_node 状态机正确走到 **WAIT_NAV_STABLE**（室内无 GPS/无视觉定位，按设计安全等待）；③ fake_perception 30Hz 契约发布正常；④ check_vision_contract 四项 PASS（30Hz/心跳/stamp/字段语义）
- **修复 `check_vision_contract` 两处（已入本仓库）**：① 定时器回调内 `rclpy.shutdown()` 会把 spin 卡死（SIGTERM 都杀不动）→ 改 Future + `spin_until_future_complete` 标准范式；② 报告 print 走块缓冲，强杀即丢 → `sys.stdout.flush()`。**教训：rclpy 回调里禁止 shutdown；工具类输出重定向到文件/管道必须 flush**
- **运维教训两枚**：① `pkill -f` 的模式若出现在自身命令行（含末尾脚本路径）会自杀，用 `[x]` 括号技巧或拆分命令；② `timeout` 只杀直接子进程，`ros2 run` 拉起的孙进程会变孤儿占住管道致 `tail` 挂起——长跑工具一律文件重定向 + `pkill -9` 兜底收尾
- **FCU PreArm 现状（台架正常现象）**：罗盘未校准、电池未接（USB 供电）、**TFmini 测距仪未连接** → 记入硬件线待办
- **udev 规则完成（踩坑一次）**：首版 ATTRS 匹配（serial+bInterfaceNumber，值核对无误）始终不匹配；改 **ENV 属性匹配**（`ID_SERIAL_SHORT` + `ID_USB_INTERFACE_NUM`，60-serial.rules 预导入）后 `/dev/cuadc-fc → ttyACM0`、`/dev/cuadc-fc2 → ttyACM1` 建成，实测经新设备名 3s 收 2066 字节 MAVLink ✓。今后 MAVROS 固定用 `/dev/cuadc-fc`。**附带发现：ModemManager 会在 udev 事件后抢占串口探测（EBUSY 约 20s）**，机载电脑应 `sudo systemctl disable --now ModemManager`（待用户执行）。TELEM2 UART 接线排查仍待做；M1 SITL 三项验收仍未跑（Windows 侧无环境，SITL 在 Jetson 上跑也可行）
