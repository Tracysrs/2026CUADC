# 工作记录 · 2026-09-05（数据与方案线 + 飞控调试）

> 本文记录 2026-09-05 两次工作会话，一日期间后半夜。
> **上半场**（§一~§八）：实拍数据分析 → 数据集构建 → 训练流水线 → 参考仓库补全 → 冠军方对照 → 方案文档整合。
> **下半场·晚间**（§九~§十）：飞控调试——刷固件 / 参数对齐新固件 / GPS 首检。
> 所有产出的权威方案见 `设计/总体方案与执行计划.md`（SSOT）。

---

## 一、实拍数据分析（train/ 文件夹）

**结论：这就是任务书 §6 等的 Jetson 端实拍数据**——无人机俯拍草地上贴危化标识的桶。

| 构成 | 数量 | 处置 |
|---|---|---|
| `0000_frame_00550.jpg` 片段抽帧 | 101 | 保留（每个片段仅 1 帧） |
| `frame_00550.jpg` 连续帧 | 234 | 保留 |
| `1_0001.jpg` 另一视频系列 | 79 | 保留 |
| 哈希命名 `.png`（电调接线图等截图） | 31 | **剔除**（与任务无关且全无标注） |

- 标注为标准 YOLO 格式（`类别id x中心 y中心 宽 高`），10 类标识（0~9）对应附件 11，每类 214~256 实例，分布均衡
- 55 张无标签 = 31 张 PNG + 25 张实拍背景帧（YOLO 自动当背景图）

## 二、数据集构建（build_real_data.py → real_data/）

- 剔除 31 张无关 PNG 后 414 张，按**视频片段**切分：train 376（含 25 张背景图）/ val 38
- 切分规则：`frame_*` 系列取中段连续帧、`1_*` 取末段、101 个单帧片段随机 10%——**连续帧不跨集，防验证泄题**
- 无标注实拍帧补空标签文件；`data.yaml` 不写 `path` 键（以 yaml 所在目录为根，换机器免改路径，11 类含 barrel 对齐 v1 检测头）
- 校验：414 图 ↔ 414 标签一一对应，无缺无孤儿；逐类分布 train 201~241 / val 9~32
- 产出：`real_data/` + **`real_data.zip`（240MB，可直接发队友训练）** + `build_real_data.py`（留作复现）

## 三、v2 训练流水线（jetson/）

- **`run_v2_training.sh`**：7 步一键脚本——GPU 检查 → 装 ultralytics → 解压校验 414 张 → 自动找 v1 权重（`v1_best.pt` → `best.pt` → `deliver_v1.zip` 自动解压 → 本机 `runs/`，都没有且传了合成包则自动先训 v1 兜底）→ §6 参数铁律微调 150 轮（`lr0=0.001 flipud=0.5 hsv_h=0.005 close_mosaic=10`）→ 逐类验收自动判读 → 打包 `deliver_v2.zip`
- **`AutoDL租卡训练指南v2.md`**：操作手册（租卡/上传清单/一键训练/取结果/停止计费/判读速查）
- 验收线写进脚本：逐类 mAP50 ≥0.90（白底三类腐蚀品/感染性物品/有毒品 ≥0.85）、漏检率 <5%，不达标类别自动标出
- 脚本已转 LF + bash 语法检查通过（Windows 写的脚本在 Linux 不报 `\r` 错）

## 四、参考仓库补全（参考/2026CUADC_HIT）

- 从 9/4 快照拉取 **33 个新提交**（287 文件变动），同步至 `9f0e56e`
- 排查"拉完目录反而变少"：当初克隆开了**稀疏检出（sparse-checkout）**，白名单没覆盖上游重组后的新目录 → `git sparse-checkout disable` 全量检出（120MB）
- 补出的新内容：
  1. **`视觉标定/`**：视觉伺服投放对准工具包（铅垂线静态标定 + 试投 MAD 修正 + 准星分离原则），与投放任务直接对口
  2. **`sim/`**：仿真包归拢（cuadc_sim + 危化识别仿真 v0.2.0）
  3. **`Re0_从零开始的CUADC生活/`**：ROS/状态机/视觉/飞控四册新人教程
  4. **`留给后人的话/`**：云台限位、强化学习投放提案 + 技术总结信
- 日志更新至 9.5（该队 8.11 北部赛区冠军，单轮最高 1328 分/最快 73s）

## 五、冠军方案对照 → 我们的改进增量

骨架（ArduPilot+ROS2 路线、18 态状态机、参数基线、感知工程五件套）此前已吸收，本次新增五件最有价值的：

1. **视觉时间戳 + odom 插值补偿**：100ms 延迟 @2m/s = 20cm 误差 > 0.10m 对准门限；SEARCH 段建筒位地图必须补偿
2. **独立安全监控进程**：五路心跳 + 磁盘余量（证据落盘会写满卡）+ 故障锁存联锁
3. **TensorRT 黑帧预热**：上场前跑一张假图，防第一帧推理超时（吃 1 分钟上场规则的红利）
4. **投放标定两步法**：铅垂线中位数静态偏置 + 真实试投中位数修正 + **MAD 作重复性指标**；对准平面要减桶高 0.30m
5. **快门 ≤1/500s + 手动锁曝光**（HIT 实测 exposure 120 / gain 64 / 50Hz 抗闪）

明确**不学**：RL 投放（HIT 负结果：PPO 70%、Q-learning 83.7% < 规则基线 92.7%）、云台限位（他们自己也排后面）、放弃 AI 侦察改纯航点（他们的权宜之计，我们双通道设计更优）。

## 六、方案文档整合

- 新建 **`设计/总体方案与执行计划.md`** 为唯一权威文档（SSOT）：第一部分方案（得分模型/6 条规则约束/架构选型/感知/状态机/标定/仿真/数据训练/比赛日/坑清单 12 条），第二部分分步执行计划（**P0 本周冲刺** + P1~P6 六个里程碑，每步带验收）
- 修正旧文档不一致：类别统一为附件 11 的 10 类（原任务清单"GHS 9 类"、算法方案里的标准 GHS 表均与实际数据集不符）
- **7 份旧文档移入 `归档/`**（算法方案/冠军队借鉴与对策/项目任务清单/数据集资源清单/PC端训练任务书/AutoDL指南v1/run_v1_training.sh），乱码文件名一并修正
- `设计/README.md` 做索引，指明"改方案只改 SSOT"

## 七、问题与解决

| 问题 | 解决 |
|---|---|
| 实拍数据混入 31 张无关截图 | 按"哈希名 + PNG + 无标签"三特征识别剔除 |
| 连续帧随机切分会泄题 | 按视频片段整段切分（build_real_data.py 固化逻辑） |
| 仓库 pull 后新目录不落盘 | 稀疏检出白名单过时，disable 全量检出 |
| v1 best.pt 本地不存在 | 脚本内置合成包兜底自动训 v1；晚间会话已确认全盘无 v1 权重并打包合成 tar（274MB）走第 5 条兜底路径 |

## 八、下一步

- [ ] **P0.1**：3 个文件传 AutoDL（real_data.zip / run_v2_training.sh / hazard_labels tar 包）→ `bash run_v2_training.sh`，4090 约 1 小时 10 元内，产出 deliver_v2.zip 并判读
- [ ] P0.2：Jetson 导出 TensorRT engine（half），实测延迟
- [ ] P0.3：启动流程加黑帧推理预热自检
- [ ] P0.4：视觉时间戳 + odom 插值落代码
- [ ] GPS 拿到室外验收（3D Fix + ≥8 星 + HDOP<1.5）+ 罗盘校准（P1-4 续）
- [ ] 台架其余外设：CAN PMU / TFmini / 舵机两路 / ELRS 接收机
- [ ] 白筒/H 圆实拍采集（每类标识补至 ≥300 张）

---

## 九、飞控固件与参数（晚间 · P1-3 完成 ✅）

### 起点状态（实测）

- 电脑识别 COM5（ArduPilot MAVLink）+ COM6（SLCAN）；Mission Planner 开着会占 COM5，跑脚本前需断开
- 飞控为**出厂状态**：装的是 Rover 固件（心跳 GROUND_ROVER），参数全默认；传感器健康（陀螺/加计/罗盘/气压计/AHRS 正常）

### 板型确认

- 实测 `board_version=0x1B59`（board_id 7001），与 V6X **v2** 固件内嵌 board_id 一致 → 硬件实锤 v2

### 固件

- 存档两版到 `设计/firmware/`（附 README：来源 / git 号 / 刷写步骤 / 刷后验收）：
  - `arducopter_beta_CUAV-V6X-v2.apj`（官方 beta，git `dbe79216`）← 已刷
  - `arducopter_CUAV_AP4.6.3.apj`（CUAV 编译稳定版，回退备用）
- 刷后验证：心跳变 `MAV_TYPE_QUADROTOR`，`flight_custom_version=dbe79216` 与存档一字不差

### 参数（当晚最大的坑）

- **这版 4.7-beta 是新一代参数命名（SI 单位后缀）+ 懒加载参数组**：
  - 改名对照：`WPNAV_SPEED`→`WP_SPD`、`LAND_SPEED`→`LAND_SPD_MS`、`FS_GCS_ENABL`→`FS_GCS_ENABLE`、`GUIDS_*` 整组取消（GUIDED 速度走 WP_SPD，倾角走 ATC_ANGLE_MAX）
  - 懒加载：`RNGFND1_*`、`PLND_TYPE` 在功能开启并重启前**不存在** → **参数必须导入两遍（中间重启）**
- 修正真 bug：`PLND_TYPE` 应为 **0**（CompanionComputer 收 LANDING_TARGET）；原文件写 1 = IR-LOCK 红外信标，架构不对
- 新增：`FRAME_CLASS=1 / FRAME_TYPE=1`（新固件默认 0=未定义，电机映射会不对）、CAN PMU 三件套（`BATT_MONITOR=8` + `CAN_P1/P2_DRIVER=1`）、TFmini（`RNGFND1_TYPE=20` + SERIAL5@115200）、`EK3_RNG_USE_HGT=4`（激光 4m 以下辅助定高，替代方案里的 PX4 参数名 EKF2_RNG_CTRL）
- `FS_THR_ENABLE=5`（RTL）为**调试期安全值——上场前必须改回 0**
- 验收：`python verify_params.py --diff 设计/V6X_ardupilot_params.param` → **34/34 全部一致**
- 参数备份：`param_backup_20260905_230343.param`（刷机默认）/ `param_backup_20260905_232322.param`（已配置基线，以后 diff 的对照基准）

### 插曲：软重启卡 bootloader

- MAVLink 软重启后飞控卡在 bootloader（COM5 消失、变成无名 USB 串口设备），**拔插 USB 恢复**，写入的参数完好
- 教训：重启后 COM 号可能变化，连不上先扫端口，不是变砖

### GPS 首检

- GPS 驱动通信正常（GPS1 口，自动配置开启）；**外置罗盘已识别并自动设为第一优先级**（符合 CUAV 手册要求）
- 室内 0 星属正常，待室外验收；罗盘校准未做，首次飞行前必须做

## 十、飞控线新增工具与坑清单

新增脚本（工作区根目录，依赖 pymavlink + pyserial）：

| 脚本 | 用途 |
|---|---|
| `check_fc.py` | 连飞控读版本/模式/电池/GPS/姿态 |
| `check_gps.py` | GPS 定位/卫星数/罗盘检测 |
| `verify_params.py` | `--export` 全参数备份；`--diff` 与参数文件逐项比对（配置一致性检查，对应方案坑清单 6） |
| `activate_lazy_params.py` | 激活懒加载参数组并重启（参数名探查用） |

飞控线坑清单（并入方案 §10.2 的候选）：

1. **4.7-beta 参数改名 + 懒加载**：参数导入必须两遍（中间重启），`verify_params.py --diff` 会揪出所有无效名
2. **PLND_TYPE=0 才是伴随计算机**，1 是 IR-LOCK
3. **软重启可能卡 bootloader**：拔插即恢复；COM 号重启后会变
4. **新固件 FRAME_CLASS 默认 0（未定义）**：必须显式设 1/1（Quad X）
