# 2026CUADC 多旋翼侦察与救援 · 总体方案与分步执行计划

> **本文件是项目唯一权威方案文档（SSOT）**，2026-09-05 整合定稿。
> 整合自：《算法方案》《冠军队借鉴与对策》（一/二/三阶段）《项目任务清单》《数据集资源清单》《PC端训练任务书》。
> 被整合的旧文档已移入 `99_归档/`，不再更新——**改方案只改本文**。
> 平台：CUAV Pixhawk V6X（ArduPilot） + Jetson Orin Nano + 单目下视相机 + TFmini 激光定高。
> 状态标记：✅ 已完成｜⏳ 进行中/就绪待执行｜⬜ 未开始。

---

## 0. 文档地图

> 2026-09-17 顶层重构为学科模块制（原 产出区/记录区/查阅区 三分区退役），本地图按新结构登记。

### 0.1 现行文档

| 文档 | 作用 |
|---|---|
| `总体方案与执行计划.md`（仓库根） | **本文件**，方案 + 执行计划的唯一权威版本 |
| `01_视觉感知/视觉算法设计.md` | 视觉线三任务（判读/找筒/H 圆）算法**详细设计**：双通道、单目解算修正口径、后处理四件、LANDING_TARGET→PLND、内参标定、风险清单（2026-09-13） |
| `01_视觉感知/数据与训练/` | v2 实拍数据集（414 张，`real_data/` `train/` `build_real_data.py`）＋训练操作手册 `v2训练流水线/AutoDL租卡训练指南v2.md`（配合 `run_v2_training.sh`，内含 v1 自动兜底）＋白筒采集工具 `barrel_seg_tools/`（含采集操作卡）＋版本台账 `deliver_v2/dataset_version.md`、`hazard_hgd_10cls_v2/dataset_version.md` |
| `02_飞控与硬件/` | 飞控参数（`V6X_ardupilot_params.param` 主参数、`V6X_offboard_params.param` offboard 快照、`参数备份/` 实测基线）＋固件存档 `firmware/` ＋调试工具 `调试工具/`（`ops.py` 统一入口：check_fc / check_gps / prearm_diag / site_geo_check / rtk_setup / check_radio / verify_params / activate_lazy_params / read_params / motor_test / servo_test / fc_* 全套，见该目录 README）＋设备参数 `设备参数/`（RTK-01.md、ZD550.md） |
| `03_机载软件/` | 自研 ROS 2 代码三包＋`接口契约.md`（视觉↔状态机↔侦察薄契约）＋`时间同步设计.md`（P0.4 取帧时刻戳+odom 插值）＋`README.md`（编译 / SITL 联跑 / M1 验收） |
| `04_仿真/SIH台架/` | SIH 台架在环仿真三件套：计划 + 操作手册 + `V6X_SIH_bench.param`——真 V6X 跑全任务 + `SIM_OH_MASK` 真舵机/电机输出，源码 pin `dbe79216` 与飞行固件同源（2026-09-15） |
| `04_仿真/仿真环境/` | Gazebo 救援仿真环境 cuadc_rescue_sim（SETUP / CONFIGURATION / RULE_MAPPING）＋ Jetson 仿真操作手册 |
| `05_操作与比赛/` | 上场操作卡：`比赛日_上场操作流程.md` ＋ `现场操作_上电到遥控手动飞行实施步骤.md` |
| `06_使用说明书/` | 成套说明书 10 册（入口 README；新人路线 00/07；09 接线图含 RTK-01-R 针脚图 SVG） |
| `07_工作日志/` | **进展权威入口**：按日工作会话记录，以最新一篇为准（目录 README 有索引） |
| `08_参考资料/`（不入库） | 外部参考：手册 / 硬件资料 / 比赛原件 / 大赛附件（附件11 危化标识为类别基准）＋ `外部仓库/` 三仓台账（HIT hgd_cudac ＋西安石油 cuadc2026 ＋demo，台账见 `08_参考资料/外部仓库/README.md`） |

### 0.2 已隔离的过时文档（`99_归档/`）

| 文档 | 隔离原因 |
|---|---|
| `算法方案.md` | 内容已并入本文 §3~§5、§10 |
| `冠军队借鉴与对策.md` | 三个阶段结论已全部并入本文相应章节 |
| `项目任务清单.md` | WBS/里程碑/风险已并入本文第二部分 |
| `数据集资源清单.md` | 已并入本文 §8（实拍数据缺口已关闭） |
| `PC端训练任务书.md` | v1 训练任务书，被 §8 训练流水线 + run_v2_training.sh 取代 |
| `AutoDL租卡训练指南v1.md` | 被 v2 指南取代（v2 脚本自动兜底 v1 训练） |
| `run_v1_training.sh` | v1 独立训练脚本，功能已内置于 run_v2_training.sh |
| `使用说明书分册版_20260912/` | 旧 15 册按设备/环节拆分的分册，被 06_使用说明书 模块化整合取代 |
| `train工作副本_20260905/` | 训练工作区历史副本，只读备查 |

---

# 第一部分 方案

## 1. 比什么：得分模型与目标

| 项 | 满分 | 说明 |
|---|---|---|
| 起飞分 | 50 | 带两瓶水飞越起飞线即可 |
| 空投分 | **800** | 两瓶分别投中 1 号筒(500) + 2 号筒(300)；投同一 A 区只算一次 |
| 侦察分 | 300 | 3 个标识全对，**错一个 −100，不填不扣** |
| 着陆分 | 50 | 螺旋桨停转 + 轴心投影落 ø80cm 圆内 |
| 时间分 | 200 | `200×(300−T)/300`，只有**完赛**才有 |

**理论上限 ≈ 1400**。T=150s 实战高分线 **1250~1300**；冲奖目标 **1250+**。

**完赛硬门槛（缺一项时间分归零）**：起飞 + 空投分 ≠ 0 + 侦察至少对 1 个 + 着陆成功 + 停表前交成绩单。
→ **侦察不是加分项，是通行证；宁可空着不能错填。**

分档：保底 ~330（一瓶 B 区 + 侦察对 1 + 落回）｜及格 ~600（一瓶进 A 区 + 侦察对 2）｜冲奖 **1250+**（1 号筒 + 2 号筒 + 全对）。

竞争格局实证（HIT 冠军队）：北部赛区冠军单轮最高 **1328 分、最快 73s**，稳定水平 900+（卡在小桶 15cm 投不中）。**1300 与 900 的差距就是小桶**——我们冲奖路线第一瓶必须敢瞄 15cm 筒，前提是 §6 标定与轴线同轴验收做实。

## 2. 规则硬约束（直接决定设计的 6 条）

| # | 规则要点 | 设计约束 |
|---|---|---|
| 1 | 通则 6.6 + 细则 5.13：设备集中管理，场上不接受维修调试 | 进场准备只有 **1 分钟**，系统必须上电即跑、零现场调试 |
| 2 | 细则 5.4：允许计算机睡眠/休眠 | Jetson **预加载模型后 suspend，唤醒即跑**（唤醒到首帧推理 <10s） |
| 3 | 细则 7.3.2：禁止赛前技术手段确定筒位 | 筒位只能机上实时检测，不许预标定 |
| 4 | 细则 7.3.4：未经允许开图传电源 = 取消资格 | 侦察主通道走数传；图传上电前必须问裁判 |
| 5 | 通则 8.5：无线电干扰申诉不受理 | 频点自己错开；不假设 RTK/ GPS"天空干净" |
| 6 | 通则 7.1.2：教师不得操作设备 | 一切按 4 名组员设计，不押宝老师 |

好消息：本项目不强制交设计报告，但通则 5.3 允许抽审 → 备一套图纸文档。

## 3. 系统架构与选型

### 3.1 架构

```
                          ┌───────── ArduPilot (V6X) ────────┐
                          │  EKF(GPS+光流+激光 TFmini)        │
                          │  速度/位置控制                    │
                          └────────────────┬─────────────────┘
                                           │ MAVLink (TELEM2, 921600)
                                           ▼
┌──── Jetson Orin Nano (ROS2) ──────────────────────────────────────────┐
│  相机 ──► 图像预处理 ──► ┌─ 桶检测 (YOLOv8n-seg, TensorRT FP16)        │
│                          │   双通道: YOLO + 传统视觉兜底               │
│                          │   时序一致性(5帧确认) + 同帧去重            │
│                          └─► 像素 ──► 单目解算(含odom插值) ──► 机体系筒位│
│                                  │                                    │
│                       投放舵机指令          标识分类(10类+拒识)         │
│  状态机 FSM(18态) ──► 分级超时/丢目标保护 ──► MAV_CMD_DO_SET_SERVO      │
│                 └────► 降落 LANDING_TARGET (ArduPilot PLND)            │
│  safety_monitor(独立进程): 五路心跳 + 磁盘 + 故障锁存联锁               │
└───────────────────────────────────────────────────────────────────────┘
```

### 3.2 选型定案

| 项 | HIT（冠军） | 我们 | 定案 |
|---|---|---|---|
| 飞控固件 | ArduPilot | ArduPilot | ✅ 不换（V6X v2 锁定验证过的 beta 版本并存档 .apj） |
| 机载电脑 | NUC 10/13 | Jetson Orin Nano | ✅ 不换（有 CUDA + 休眠唤醒红利；NUC 路线证明性能焦虑没必要） |
| 相机 | D435i 深度 | 单目 + 已知筒高 | ✅ 维持单目：他们小桶失手根因之一恰是深度外参用错；单目标定链更短。若实测撑不住小桶 CEP 再议（预案不预换） |
| 定高 | — | TFmini 激光（~50 元） | ✅ 气压计 2~5m 桨风 ±0.5m 跳变必败；EKF2_RNG_CTRL=1，4m 以下激光辅助 |
| 定位 | CUAV C-RTK 2HP | 普通 GPS | ✅ 维持纯 GPS+视觉。论据：他们 RTK Fix 收敛实测 3h37m，区赛失利与 RTK 链路相关（survey-in 精度 2.49m 勉强过线、基站日志早停 2 分 21 秒） |
| 侦察 | 区赛纯航点+人眼 → 国赛 DJI O4+N3 眼镜 | AI 分类 + 数传回传 | ✅ 保留 AI 主通道（填单快=停表早），**人眼 FPV 作备份降级**（硬件首选 DJI O4 + N3，已验证选型）；图传上电先报备裁判 |
| 仿真 | Gazebo Harmonic + SITL | 无 → 建 | ✅ 用他们开源包搭（见 §7），"自动飞行代码必须先过 SITL 再上真机" |

### 3.3 四人分工

| 角色 | 负责 | 主交付 |
|---|---|---|
| 组长（操纵员） | 总体 + 飞控调参 + 上场操作 | 能飞、能落、不炸 |
| 结构/动力 | W1+W2+W7（§13 P1/P3） | 机架、桨保、投放机构、标定 |
| 机载软件 | W4+W6（§13 P1/P4） | Orin 环境、MAVROS 状态机、安全监控、地面站 |
| 视觉算法 | W5+数据集（§13 P0/P2） | 检测/分类模型、TensorRT 部署、标定工具 |

## 4. 感知方案

### 4.1 白筒检测（双通道 + 时序）

| 通道 | 实现 | 说明 |
|---|---|---|
| 主 | YOLOv8n-seg，TensorRT FP16，imgsz=640 | Orin 上 2~4ms/帧，验收 ≥25FPS、实拍 ≥95% 检出 |
| 副 | LAB 的 L 通道高 + A/B 近中性 → 阈值 + 形态学 → `cv2.fitEllipse` | 光照异常兜底，与主通道 IoU 匹配降置信 |

后处理四件（参数已按 HIT 实测基线固化，见 §5.4）：
- **时序过滤**：连续 5 帧、位置 std < 阈值才确认，取中位数；
- **同帧去重**：双框中心距 <2cm 合并（HIT 区赛同桶拆两目标、两瓶投一点的教训）；
- **独立性门控**：两筒水平距离 ≥0.20m 且直径差 ≥0.025m 才算不同目标，**参数必须真正参与判断**（HIT 的直径判据写了没生效）；
- **直径物理先验**：反算物理直径必须落 0.08~0.35m 并能对号 15/20/25cm，否则拒检——免费的一致性校验，还能反推筒号辅助决策。

### 4.2 坐标解算（单目）

- 测距 `Z = f_px × H / h_px`（H=筒高 0.30m）；横向 `dx=(u−cx)·Z/fx`，`dy=(v−cy)·Z/fy`；按锁定的 yaw 旋到世界系。
- **对准平面要减桶高**：瞄准的是桶口平面，`alignment_z = 目标参考高 − 0.30m`（HIT 标定工具 DESIGN.md 的坑）。
- 高度只用飞控相对高度（TFmini 辅助），不用气压。
- 起飞前锁航向（整个任务不转机头），起飞前读一次 odom 存**任务原点**——EKF origin ≠ 起飞点，HIT 在此翻车两个月。

### 4.3 时间同步与延迟补偿（本次新增，勿漏）

- 感知消息带**取帧时刻**的 `header.stamp`（不是发布时刻）；
- 状态机用 **odom 历史对视觉帧做插值夹逼**（容差：pipeline delay 1.5s、未来戳 0.05s、最大插值间隔 0.2s、起飞前至少 3 帧视觉）；
- 量级论证：搜索段 2 m/s × 100ms 延迟 = 20cm 位置误差 > 0.10m 对准门限。投放前近乎悬停影响小，但 **SEARCH 段建筒位地图必须补偿**，否则筒的世界坐标整体滞后。

### 4.4 危化品标识分类（侦察判读）

**权威类别 = 附件11 的 10 类**（与训练集 data.yaml 一致，勿用标准 GHS 类别表）。

⚠️ **2026-09-12 二次勘正**：本表此前误植为标准 GHS 集合（感染性物品/氧化剂/易燃液体），
已按附件11 扫描件**逐页转图比对**定稿下表（页序 = id 序），与 deliver_v2 训练集、
HIT 仿真纹理集三方一致。**旧表 id 2~5 含义错位**（旧2腐蚀品→新4、旧3刺激性→新2、
旧4感染性物品≈新5生物危害、旧5氧化剂在附件11 中不存在），引用旧模型/旧文档的
id 前必须先对本表。

| id | 类别 | 附件11 图案 | 易混提示 |
|---|---|---|---|
| 0 | 爆炸品（橙） | 菱形，编号 1.5 | 唯一橙色 |
| 1 | 不燃气体（绿） | 菱形，编号 2 | 唯一绿色 |
| 2 | 刺激性（白底 X） | 白底大 X + IRRITANT | 白底系，靠 X 图形区分 |
| 3 | 放射性物品 | 三叶，上/下半分幅 | 图案唯一；与刺激性低对比互混（v2 实测 8%），补数据重点 |
| 4 | 腐蚀品（白底） | 两试管浇蚀手，上/下半分幅 | 白底系 |
| 5 | 生物危害（白底） | 三环 biohazard | 图案唯一 |
| 6 | 遇湿易燃物品（蓝火焰） | 火焰，编号 4 | 火焰三件套之一，靠颜色 |
| 7 | 有毒品（白底骷髅） | 骷髅（无菱形框） | 白底系 |
| 8 | 自燃物品（火焰） | 火焰，上/下半分幅，编号 4 | 火焰三件套之一，靠颜色 |
| 9 | 易燃（红火焰） | 红底火焰 + FLAMMABLE | 火焰三件套之一，靠颜色 |

白底系 = 刺激性/腐蚀品/生物危害/有毒品（+放射性物品下半幅），是判读互混主战场；
火焰三件套 = 遇湿易燃/自燃/易燃，靠底色区分（拒识 margin 的存在理由）。

- **端到端 YOLOv8n-det 检 10 类**（不两阶段），**颜色是第一特征，灰度必败**；
- **拒识策略（最关键）**：top1 < τ=0.7 → 留空；top1−top2 < margin=0.3 → 留空（火焰三件套常见）。留空 0 分，错填 −100；
- **双通道**：AI 主通道（数传回传+地面站显示，组员填单）；人眼 FPV 备份（图传先报备）。AI 置信不足或数传异常切人工；
- **证据落盘**：侦察态 1s/张 JPEG + 每条结果存"裁剪图+类别+置信度"JSONL（原子写），申诉/复盘两用；**分类 ROI 从全分辨率原图裁**，不在 640 缩图上裁；
- 相机参数基线：快门 ≤1/500s（fx=800、6m/s、1/250s 模糊 6.4px 的定量结论），**手动锁曝光**（HIT 实飞值 exposure 120 / gain 64 / 50Hz 工频抗闪），禁自动曝光。

### 4.5 精准降落

ArduPilot PLND 模块，Orin 只发 `LANDING_TARGET`（angle_x/y + distance，BODY_NED）。飞控参数已入 `V6X_ardupilot_params.param`：`PLND_ENABLED=1、PLND_TYPE=1`。主检测 YOLOv8n 单类 h_marker（自制 ø80cm 数据 ~200 张）+ Hough 圆兜底；1.5m 高发 `MAV_CMD_NAV_LAND_TARGET`。

### 4.6 感知工程五件套 + 契约

1. 每帧发布心跳（空结果也发），任务侧按新鲜度判故障，故障**停发 = fail-closed**；
2. 模型文件 SHA-256 校验，防错拿错版本；
3. 外参/标定正交性校验（HIT 小桶失手教训），错误阻断启动；
4. 逐检测质量门控 + 拒绝原因计数（深度/几何/物理边界等 9 项的检测版：置信度、掩码/框有效性、直径物理边界等）；
5. 图像-odom 时间同步（§4.3）；
6. 视觉→控制接口用**薄契约**：PoseArray（机体系），orientation 字段复用为置信度/尺度并带版本哨兵；配 `check_vision_contract.sh` 式的契约校验脚本。

## 5. 飞行与状态机

### 5.1 全生命周期 18 态

```
WAIT_FCU → WAIT_NAV_STABLE → LOCK_FRAME(航向2s内变≤2°) → PRESTREAM(预发setpoint 1.5s)
→ WAIT_GUIDED(飞手切GUIDED=最终确认) → WAIT_ARM → TAKEOFF(4.0m, CommandTOL)
→ SEARCH(2.0m, 等3独立筒) → ALIGN(1.8m) → RELEASE(八门控)×2
→ RECON_CLIMB → RECON_SURVEY(蛇形6航点) → RETURN_CLIMB → RETURN_HOME(4.0m)
→ LAND(0.7m/s) → DISARM(独立态: 相对高≤0.30m+速度门限+稳定1.5s后自动上锁)
→ DONE；旁路: PILOT_OVERRIDE(飞手切走模式,程序立即退让) / ABORT(致命故障)
```

要点：起飞用 CommandTOL 到 0.9×目标高才切 setpoint；**飞手切 GUIDED 才启动 + 程序绝不与飞手抢权**（HIT 全版本铁律）；DISARM 独立成态是规则 6.1.4"桨停转才有着陆分"的要求。

### 5.2 目标锁定与投放

**锁定五步**（SEARCH 态）：3 独立筒确认（≥5 帧 + 位置 EMA α=0.25 + 直径中位窗 9）→ 独立性门控（≥0.20m / 直径差 ≥0.025m）→ 直径升序对照 15/20/25cm 物理先验 → 排名稳定 0.8s 才锁 → `drop_order` 参数化：`conservative`（先 25cm 保底，训练期）/ `aggressive`（直攻 15+20cm 冲 800，成绩稳定后切）。已投筒 0.25m/120s 拉黑。

**投放策略（冠军队实战版，勿改回持续伺服）**：
2m 粗对准 → **冻结目标世界坐标 + 冻结飞机位置** → 速度/姿态稳定 0.5s → 立即投放。下降段不依赖持续跟踪 ID；丢视觉重捕获一次，仍丢**放弃该桶**——禁止按冻结旧坐标盲投（HIT 区赛教训）。降级预案：SEARCH 超时允许"2 筒 + 1 未知"降级投放，不死等 3 筒。

**释放八项门控**（全过才投，任一超时 6s → 弃桶）：
水平/垂直误差 ≤0.10m｜稳定 ≥0.8s｜水平速度 ≤0.08m/s｜垂直 ≤0.05m/s｜倾角 ≤5°｜偏航误差 ≤5°｜释放前保持 1.5s（弹道补偿 `Δ=v·√(2h/g)`，悬停时 ≈0，保留 sanity check）。

**舵机**：`MAV_CMD_DO_SET_SERVO`（SERVOx_FUNCTION=0，AUX 通道 9/10），50Hz + `PWM_AUX_DISARM` 可地面测试；指令到动作 <200ms；两路独立。

**视觉伺服**（对准段）：发速度指令，`Kp=0.003 px→m/s`、死区 5px、限幅 0.8m/s；⚠️ ArduPilot GUIDED 速度 setpoint 无断流超时——伺服环 ≥10Hz + 500ms 看护，超时切位置保持。

### 5.3 独立安全监控（本次新增）

safety_monitor **独立进程**，不与任务节点同生命周期：
- 五路心跳：MAVROS 1.5s / odom 0.6s / 视觉 1.5s / 载荷 2.0s / 任务 2.0s；
- 任务 900s 硬超时；**磁盘剩余 <512MB 告警**（证据落盘 1s/张会写满卡）；
- 故障锁存（fault_latched）+ SafetyStatus 联锁：视觉心跳丢失即禁投，导航健康则返航否则受控降；
- 各环节超时一律 `fail_and_return()`（先返航再降）；另有需交互确认的人工紧急脚本入口。

### 5.4 参数基线表（HIT 实测值，按我们平台实测后修订）

| 参数 | 值 | 参数 | 值 |
|---|---|---|---|
| 起飞/返航高度 | 4.0 m | 转场/搜索/侦察速度 | 4.0 / 2.0 / 3.0 m/s |
| 搜索高度 | 2.0 m（4.2m 检不出桶） | 投放进近/释放定位 | 0.7 / 0.25 m/s |
| 粗/精对准 | 1.8 m / 门限 0.15/0.08m | 航点接受半径 | 0.40 m |
| 对准稳定/超时 | 0.8 s / 12 s | 任务总超时 | 240 s（比赛 5 分钟） |
| 释放八门控 | 见 §5.2 | 着陆确认 | ≤0.30m + 速度门限 + 1.5s |
| 航向锁定 | 2s 内变 ≤2° | 视觉起飞门禁 | 起飞前 ≥3 帧有效 |
| 桶记忆/已投拉黑 | 120 s / 0.25m | 确认帧数/EMA | 5 帧 / α=0.25 |
| 舵机 PWM | 收拢 1100µs / 释放 1900µs / 保持 0.7s | 舵机指令延迟 | ~46ms ACK，0.8s 回仓 |
| 搜索/侦察区几何（场地系） | 仿真场地：搜索 x27.5~32.5 / y±4，6 带；侦察带位 x52.5~57.5、沿 y±4 扫，3 带（真机按赛场实测修订） | 搜索带覆盖判据 | 带间距/2 ≤ 1.055×高度（下视 hfov1.5rad、848×480 → 带间无盲区） |

### 5.5 逐段时序预算表（2026-09-12 新增，兑现 P5-4"时间分解表优化"）

> 计算基准：下视相机 hfov 1.5rad / 848×480；航线 = `mission_params.yaml` 当前值
> （搜索区 6 带、侦察区沿 Y 3 带，见 §5.4 几何行）；时长 ≈ 距离/速度（smoothstep
> 插值不改变总距离）。仿真无视觉版实测基线 96s（09-11 日志）。

| 状态段 | 距离/动作 | 速度 m/s | 预算 |
|---|---|---|---|
| TAKEOFF | 爬升 4m + 稳定 | — | ~7 s |
| SEARCH 转场 | 起飞点→搜索区入口 ≈28m | 4.0 | ~7 s |
| SEARCH 蛇形 | 6 带×5m + 5 次×1.6m 换带 = 38m | 2.0 | ~19 s |
| ALIGN+RELEASE ×2桶 | 带内移动 ≤5m + 粗/精对准 + 稳定0.8 + 门控2.3 + 舵机0.7 | 0.7/伺服限幅 | 12~17 s/桶 → 25~35 s |
| RECON_CLIMB | 2→4m | 4.0 | ~1 s |
| RECON_SURVEY | 入场 ≈26m + 3 带×8m + 2 次×2.5m = 29m + 拍照 6×(0.1~1s) | 3.0 | ~19~25 s |
| RETURN_CLIMB + HOME | 返航 ≈58m（高度已在 4m） | 4.0 | ~15 s |
| LAND + DISARM | 降落确认 + 桨停 | — | ~17~22 s |
| **合计（无视觉 M1）** | | | **≈87 s** |
| **合计（M3 含投放）** | | | **≈112~122 s** |

预算结论：M3 全流程 ≈2 分钟，对 **150s 冲奖线留 ~30s 余量**，对 240s 硬超时余量充足。
超预算优先观察：投放段（门控超时弃桶 +6s/次）、LAND（SITL 实测 20s）、SEARCH 降级触发。

> **仿真实测锚点（2026-09-12，M3 判分闭环首验）**：全程 **129s**、投放 **2/2 全 A 区**
> （误差 6.2/6.8cm）、拍照确认 6/6。SEARCH 实测 33s（预算 26s，差 = 锁定排名稳定窗 +
> 转场），其余段与预算吻合。验收脚本 `fc_sitl_m3.sh`，排障详见当日工作日志。


## 6. 投放标定（两步法，本次新增章节）

> 工具在 `08_参考资料/外部仓库/hgd_cudac/视觉标定/`，配合我们的单目 Vision Provider（输出机体系目标）即可用。

1. **静态标定（铅垂线法）**：飞机低位固定 → 从投放机构中心垂铅垂线 → 人工移动整机让线压目标中心 → 保持 ≥3s → 该窗口目标 FRD X/Y **中位数** = 该投放口静态偏置（Z 由机械测量）。工具：`tools/estimate_static_offsets.py`。每口独立（HIT 冠军版两口偏置差 5cm 量级）。
2. **动态修正（真实试投）**：每载荷每高度 ≥3 次真实试投，`最终偏置 = 静态偏置 + 落点偏差中位数`，输出 **MAD 作重复性指标**——MAD 大 = 问题在机械/延迟不在算法。工具：`record_drop_trials.py` + `solve_drop_trials.py`。覆盖舵机延迟、瓶体摆动、残余速度、气流。
3. **两个准星分离原则**：目标准星与**载荷真实投放中心投影**准星分开画；载荷准星只用飞机固有标定值，绝不能用目标位置生成（否则准星跟着目标跑，对准误差被隐藏）。做调试显示界面时照此实现。
4. **验收**：目标高度落点 **CEP < 5cm**（冲 15cm 筒的必要条件）+ 舵机推力轴线与水瓶质心同轴 <5mm（图纸评审项——HIT 小桶投不中的机械根因）。

## 7. 仿真与测试

- 选型定案：**Gazebo Harmonic + ArduPilot SITL + MAVROS**（HIT 踩坑结论：Classic 11 配置失败、Fortress 兼容差）。包：`08_参考资料/外部仓库/hgd_cudac/sim/cuadc_sim`（全流程，1:1 场景+随机场景生成+规则映射 `docs/RULE_MAPPING.md`）+ `cuadc_hazard_recognition_sim`（侦察识别小场景）。
- 铁律：**每版状态机先过 SITL 再上真机**；真机前还有拆桨预检流程。
- 四项验收（含本次新增第 4 条）：
  1. 固定 seed 随机场景 + `generated_scene.yaml` 真值对比；
  2. 仿真与实机共用 `/perception/*` 接口（sim-to-real 零改码）；
  3. ≥30 轮随机布局全流程回归；
  4. **异常注入**：丢 odom / 丢检测 / 心跳超时 / 磁盘告警，逐一验证保护逻辑与安全落地（HIT 赛后自评"仿真缺异常注入与自动回归"，我们一步到位）。
- HIT 选型教训直接抄：iris 模型实名 `iris_with_ardupilot`、等飞控 Ready 再起 MAVROS、Wayland 需 `QT_QPA_PLATFORM=xcb`（详见归档日志引用/`日志.md` 2.19）。

## 8. 数据与模型训练

### 8.1 资产现状

| 资产 | 状态 |
|---|---|
| v1 合成集 `01_视觉感知/数据与训练/v2训练流水线/hazard_labels_v2_11class/`（2220 张/11 类，含 barrel） | ⚠️ 数据完整但**类别表为旧 GHS 表**（§4.4 勘正），危化 10 类部分须按图案重映射核验后才可用于训练；**barrel（10 号类）与类别表无关，仍是 P2 白筒检测的资产** |
| **v2 实拍集**（无人机俯拍标识桶，445 张原片） | ⚠️ 414 张切分（train 376/val 38）仍有效，但 `real_data` 的 data.yaml 与标签是旧 GHS 表——**已被按图案重映射版取代**（见下行），旧标签作废勿直接训练 |
| **`hazard_hgd_10cls_v2`（deliver_v2，09-12 交付）** | ✅ **当前权威数据/权重**：v2 实拍集按图案重映射为附件11 十类 + 修正 12 处错标（数据在另一工作区 `mapped_dataset`，权重与报告在本仓 `01_视觉感知/数据与训练/deliver_v2/`——git 白名单外，注意另行备份）；mAP50=0.963 / P=0.907 / R=0.956，未达标：刺激性 0.853 / 放射性 0.858 |
| v1 训练（hazard_synth_v1） | ❌ 废弃：类别表错误 + COCO 预训练直训路线已验证，无需合成预训练 |
| v2 微调链（hazard_mix_v2 / `run_v2_training.sh`） | ⏸️ 停用：其上游（v1 合成 + real_data 旧标签）均为旧表；数据重映射后如需可复活。现行路线见 §8.2 |
| 白筒实拍 / H 圆实拍（~200 张） | ⬜ 缺口，买齐器材后拍 |

公开数据集结论：危化标识类公开集与比赛附件类别体系不一致，**只当背景负样本，不直接训练**；helipad 集可做 H 圆预训练初值；白筒无对口公开集，走合成+实拍。

### 8.2 训练流水线（09-12 定案）

> **现行路线 = COCO 预训练直训**，已在 deliver_v2 全流程验证。原"v1 合成预训练 →
> 实拍微调"链（`run_v2_training.sh` + AutoDL指南v2）因上游类别表为旧 GHS 表**停用**，
> 数据重映射后如需微调可复活；指南留作租卡/取件操作流程参考。

```
按图案重映射的 10 类实拍集（附件11 表，参照 hazard_hgd_10cls_v2 的映射与备份惯例）
        ▼
yolov8n COCO 预训练直训（100ep, imgsz=640, batch=16, seed=0；GPU 上可加
        flipud=0.5 / hsv_h=0.005 / close_mosaic=10，色相即类别特征勿放宽）
        │  best.pt + val_report.txt + 混淆矩阵 + dataset_version.md（= deliver 包定式）
        ▼
逐类验收（mAP50≥0.90 无豁免，漏检率<5%）→ 未达标类补数据迭代 v3
        ▼
交付 Jetson → TensorRT engine（在 Jetson 上导出，绑定本机 TRT 版本）
```

- deliver_v2 实测：CPU 直训 100ep 即达 mAP50=0.963；未达标 刺激性 0.853 / 放射性 0.858、
  召回<0.95 三类（刺激性/放射性/有毒品）→ v3 补拍这两类（蓝地场景）+ 蓝地背景负样本。
- 小目标漏检普遍偏高时试 `imgsz=1280` 重训，先和部署端对齐推理尺寸。
- data.yaml 不含 `path` 键（以 yaml 所在目录为根），换机器免改路径。

### 8.3 数据集版本管理

- 命名：`hazard_hgd_10cls_v2`（当前，deliver_v2）→ `hazard_hgd_10cls_v3…`，数据快照与权重同名存档；训练交付包自动带 `dataset_version.md` + data.yaml 快照（deliver_v2 已按此定式交付）。
- **现场实拍验证集必须独立保留**（模拟比赛当天筒和光照），微调数据切分已按视频片段防连续帧泄漏；后续新批次数据沿用 `build_real_data.py` 的切分逻辑。

### 8.4 后续采集

1. 白筒三种规格在真实地面（蓝底/水泥地）实拍含斜视角；2. H 圆 ø80cm 自制 ~200 张；3. 标识实拍**优先补 刺激性/放射性物品（v2 未达标两类，蓝地场景）+ 蓝地背景负样本**（v2 混淆矩阵背景误报集中于此），再补至每类 ≥300（当前 ~240/类）；4. 每批新数据跑 §8.2 流程迭代 v3。

## 9. 比赛日流程与合规

### 9.1 上场 1 分钟流程（全程 ≤60s，不敲命令）

提前：Jetson 预加载模型 → **黑帧推理自检/预热**（TensorRT 首帧有秒级初始化，上场前跑一张假图——本次新增）→ suspend。上场：放机 → 装瓶 → 按计时器 → 开机唤醒 → 报备图传电源（如用）→ 飞手待命切 GUIDED。

### 9.2 上场前检查表

- [ ] 冷启动验证自启（**不能 SSH 手拉**，HIT 5.8 事故）
- [ ] **机械紧固检查**：电机螺丝、桨保、限位（HIT 9.5 电机飞出炸机）
- [ ] 舵机两路地面动作测试 + 瓶在位
- [ ] 上电后机头不动核对锁头航向（罗盘漂移疑似 HIT 一机组失利根因）
- [ ] `preflight_only` 只读预检：模型/标定/配置/设备四项校验，打印生效配置摘要并核对（防 YAML 与代码脱节）
- [ ] 数传频点与邻场错开；磁盘剩余 >512MB
- [ ] 电池标签、安全开关演示功能正常（细则 5.7 当裁判演示）

### 9.3 合规

逐条对照技术要求 2.1~2.7 自检签字；图纸/电气图/软件架构图备查（通则 5.3）；组长操纵员标识（通则 3.7）；报名约束（每单位 ≤2 机组等，截止后不可换人）；赛后立即断电交还。

## 10. 不做的事与坑清单

### 10.1 明确不做

| 不做 | 原因 |
|---|---|
| 纯灰度模型 | 标识首特征是颜色，必败 |
| OCR 读中文类别名 | 4m 高度字只剩 ~10px，必败 |
| 自写姿态环/降落速度环 | ArduPilot 成熟模块（PLND）不重写 |
| 不带兜底的纯 YOLO | 光照异常时模型抽风 |
| 假设 RTK 可用 | 收敛 3h37m + 干扰申诉不受理 |
| Jetson 上训练 | PC/AutoDL 训完导出 engine |
| **强化学习投放** | HIT 已试错：PPO 70%、Q-learning 83.7% < 悬停规则基线 92.7%，负结果，先研究延迟与标定 |
| 云台限位（现阶段） | HIT 亦列为"下次再做"；除非实测 CEP 被机身姿态卡死 |
| D435i 深度相机（现阶段） | 见 §3.2，预案不预换 |
| K230/CanMV 视觉模块 + 二维电动云台（09-13 评估否决） | 与"相机只是传感器、脑子在 Jetson"架构互斥：换它推理 = 权重重转换 + 契约/systemd 全套重写；只当相机用 = 白买 SoC 多一跳延迟；WiFi 图传在双组同场 + 2.4G FS-i6 环境不可靠；云台对全正下视任务（找筒/H圆/标识在筒内）无收益。**维持 BL-500W-335 不换**，预算优先投白筒/H 圆实拍 |

### 10.2 坑清单（评审 checklist，源自 HIT 实际翻车）

1. 坐标系三件套：任务原点记录、ENU/罗盘航向转换（`yaw_ENU=90°−compass_hdg`）、锁航向——最大坑，翻车两个月；
2. 同帧去重/独立性判据**必须真正生效**（HIT 参数写了没用，两瓶投一点）；
3. 丢视觉禁止按冻结坐标盲投；
4. 搜索高度 2.0m 起步 + 手动锁曝光；
5. 舵机 ACK ≠ 命中：载荷在位检测 + 轴线同轴评审；
6. 配置与代码一致性：启动时打印生效配置核对（HIT 航线 YAML 3 条代码硬编码 2 条）;
7. 上场流程冷启动验证自启；
8. MAVROS 启动流精简防串口拥堵；odom 断流 >1s 切位置保持并告警;
9. RTK 基站（若用）全程禁动——"Fixed 但整体偏移"更危险，EKF 自信地信错位置;
10. 调参：AltHold 稳定 → AUTOTUNE，大风天不 tune，不全信 AI 调参;
11. 时间戳：视觉帧带取帧时刻 + odom 插值夹逼（§4.3）;
12. 外场紧固检查（电机螺丝）+ 磁盘余量监控。

---

# 第二部分 分步执行计划

> 每步格式：**动作 → 产出 → 验收**。里程碑 M1~M6 为骨架，P0 为本周可立即执行的冲刺。

## 11. 里程碑总览

| 里程碑 | 目标 | 完成标志 | 状态（2026-09-18 核，明细见 07_工作日志） |
|---|---|---|---|
| M1 能飞 | GUIDED 起降+预设航线，无视觉 | 起飞 50 + 着陆 50 到手 | 🔶 仿真全任务通（SITL 三项验收 09-07、全任务 DONE 09-12）；真机解锁/校准/电机全通（09-12）；**预设航线全自主真机首飞 ⬜**，飞控 USB 三次异常待排除（09-14，换线换口必测） |
| M2 能看 | 检测模型部署上机 | 机载 ≥25FPS 实时框选，识别准确 | ✅ 主体达成：deliver_v2 mAP50=0.963 + Jetson **56.1FPS**（09-12）+ systemd 自启 9s 就绪 + 真相机 30FPS 上机（09-13）+ 判读节点全链通；尾巴=刺激性 0.853/放射性 0.858 未达 0.90 → v3 补数据；桌面特写=训练分布外 |
| M3 能投 | 视觉对准+投放，CEP 达标 | 20 次投放 ≥80% 进 A 区（CEP<5cm） | 🔶 仿真两轮 2/2 全 A 区、CEP 5.2~6.9cm（09-12，未达 <5cm 且未满 20 次统计）；舵机 A1/A2 飞控侧验收通（09-14）→ 本体动作验收通：Holybro UBEC 6.0V 档注 A 排母线，A2 虚插排障后两路动作实测确认（09-18，详 07_工作日志/2026-09-18）→ 真机投放 ⬜ |
| M4 能连 | 全流程状态机无人工干预 | 完整任务 T ≤180s | 🔶 仿真全自主 129/135s（09-12）但终验收挂起（09-13）；P0.4 时间戳插值 ⬜；真相机×状态机×判读联调 ⬜ |
| M5 能稳 | 连续 30 轮随机布局 | 零终止零零分，平均 ≥1000 | ⬜（judge 真值参数化 + 固定 seed 回归工具链已有雏形） |
| M6 能赛 | 1 分钟上场流程+合规 | 演练通过、图纸齐备 | ⬜ 文档侧已备（上场操作卡两份 09-13），演练/图纸包未动 |

数据/模型线（与上并行）：v2 训练 ✅（09-12 deliver_v2，mAP50=0.963）→ **刺激性/放射性补数据 + v3 重训 ⬜** → Jetson 部署 ✅（09-12 用 deliver_v2/best.pt 重建 FP16 engine，56.1FPS 双验收 PASS，见 §12 P0.2）→ 现场独立验证集评估 ⬜。（原 v1 合成预训练链因旧类别表废弃，见 §8.1/§8.2）

## 12. P0 本周冲刺（就绪即可执行）

| # | 动作 | 产出 | 验收 | 状态 |
|---|---|---|---|---|
| 0.1 | **跑通 v2 微调**：AutoDL 租 4090，上传 `real_data.zip` + `run_v2_training.sh`（+ v1 best.pt 或合成包），执行一键脚本 | `deliver_v2.zip`（best.pt + 逐类报告 + 混淆矩阵 + dataset_version.md） | 判读表全部达标：逐类 mAP50≥0.90、白底三类≥0.85、漏检<5%；未达标按报告补数据迭代 | ✅ 09-12 完成（路线偏差：未走 AutoDL 微调链，另一工作区 COCO 直训 100ep + 按图案重映射数据，产出本仓 `01_视觉感知/数据与训练/deliver_v2/` 全套）。未全达标：刺激性 0.853/放射性 0.858，召回<0.95 三类 → v3 补数据（§8.2）；连带勘正 §4.4 类别表 |
| 0.2 | **Jetson 部署**：best.pt 传 Jetson，本机导出 TensorRT engine（half），实测延迟 | engine + 延迟记录 | 首帧（含黑帧预热）<10s，稳态 ≥25FPS | ✅ 09-12 完成：01_视觉感知/数据与训练/deliver_v2/best.pt 重建 FP16 engine（detect 10 类，8.9MB，`~/cuadc_models/best.engine`），实测 **56.1FPS**、预热后首帧 17ms、冷启动 1.7s，双验收线 PASS；真图冒烟通过（类别名随 engine 内嵌新表）。权重 SHA-256 三端一致，归档 `~/cuadc_models/archive/` + 本仓 `01_视觉感知/数据与训练/hazard_hgd_10cls_v2/` |
| 0.3 | **黑帧预热自检**：启动流程加 CUDA 假图推理一步 | 启动脚本更新 | 冷启动到可用 <60s，第一帧检测不超时 | ✅ 09-12 完成：`cuadc-perception.service` 上线（enabled+active），开机自动加载 engine + 3 帧黑帧预热，**重启复验通过**：本次开机 systemd Started→引擎就绪 9s（验收 <60s），内核+用户态 15.9s，NRestarts=0，`/hazard_recon` 在 DDS 图上；engine SHA-256 强校验不符拒启（fail-closed） |
| 0.4 | **时间戳插值设计**：感知消息带取帧时刻，状态机 odom 插值夹逼（§4.3 参数照抄） | 接口约定 + 代码 | 仿真注入 200ms 人工延迟，筒位地图滞后误差 <5cm | ⬜ |
| 0.5 | **确认 v1 best.pt 下落**：上次 AutoDL 实例是否已训 v1/下载 deliver_v1.zip | 权重归档 | `v1_best.pt` 入库并在 0.1 使用 | ✅ 已确认：本地无 v1 权重（2026-09-05）；已打包 `01_视觉感知/数据与训练/v2训练流水线/hazard_labels_v2_11class.tar.gz`，0.1 执行时走脚本自动先训 v1 兜底 |

## 13. P1~P6 分步计划

### P1 平台与能飞（W1/W2/W3/W4 → M1）

1. 机架（轴距 ≤550mm）+ 桨保（强制）+ 双瓶挂载 → 量测验收：对角线 ≤550mm、带载/空载重心偏移 <2cm；
2. 动力：6S 带标签电池、CAN PMU 校准（读数误差 <5%）、续航余量 ≥30%、**Orin 独立供电**（舵机动作不重启，20 次压力测试）；
3. V6X v2 刷锁定版本 ArduPilot（存档 .apj）→ 导入 `V6X_ardupilot_params.param`（含 PLND、EKF2 激光、WPNAV）→ TELEM2@921600 接 Orin、TELEM1 接数传（09-11 端到端验收通过）、频点表；
4. GPS/罗盘校准 + **安全开关人工断动力演示**（细则 5.7，过不了直接淘汰）；
5. Orin：JetPack 6.x + ROS2 Humble + MAVROS（apm.launch）+ Jetson 专用 torch + **休眠唤醒 <10s 到首帧** + systemd 自启；
6. 坐标系约定落地（§5.1/§10.2-1）+ 状态机基础：MAVROS GUIDED 起降/航线脚本（含 setpoint 看护）；
7. **验收 M1**：无视觉全流程（起飞→预设航线→降落）拿到起飞+着陆分，用时 ≤40s。

### P2 能看（W5 → M2，数据先行贯穿全程）

1. 沿用 0.1/0.2 产出的标识模型；白筒 seg 模型训练（v1 barrel 2200 框 + §8.4 白筒实拍微调）；
2. 感知节点工程五件套 + 薄契约（§4.6）+ 时序过滤/去重/独立性/直径先验（§4.1）；
3. 传统视觉兜底通道（LAB+椭圆拟合）与主通道 IoU 融合；
4. 实拍验证集评估（独立于训练集，模拟赛场光照）；
5. **验收 M2**：机载 ≥25FPS；实拍白筒检出 ≥95%；标识逐类 ≥0.90；拒识策略生效（低置信留空）。

### P3 能投（W7 + §6 标定 → M3）

1. 投放机构：两路独立舵机+释放爪；**轴线与瓶质心同轴 <5mm**（图纸评审）；
2. **标定两步法**：铅垂线静态中位数 → 试投 ≥3 次中位数修正 + MAD（§6，用 HIT 工具脚本）；
3. 目标锁定五步 + drop_order 参数化 + 释放八门控代码化（§5.2，参数照 §5.4 基线）；
4. 投放高度实测：2m 起步，低准 vs 下洗吹倒筒三档权衡；
5. **验收 M3**：CEP <5cm（≥20 次统计）；conservative/aggressive 两序均可跑通；不重锁已投筒；超时弃桶保安全。

### P4 能连（W6 + §5 → M4）

1. FSM 18 态全实现（含 PRESTREAM/LOCK_FRAME/DISARM 独立态、PILOT_OVERRIDE/ABORT 旁路）；
2. **独立 safety_monitor**（§5.3：五路心跳+磁盘+锁存联锁+fail_and_return）；
3. 分级超时 + 丢目标保护（原地小幅搜索→返航）+ "2 筒+1 未知"降级；
4. 侦察段：航点蛇形 + AI 分类 + 数传回传 + 证据落盘 + 人眼备份切换演练（✅ 09-12 真判读节点 `hazard_recon_node` 上机：engine→门控→窗口融合→判读发布+握手+证据 JSONL 全链实测通，99 帧确认判例；待接真相机与 M4 状态机联调）；
5. 弹道补偿 sanity check + LANDING_TARGET 精准降落联调；
6. **验收 M4**：全流程无人工干预 T ≤180s；各故障注入（SITL）均安全落地。

### P5 能稳（W8 + §7 → M5）

1. SITL 环境搭成（HIT 包），异常注入四项 + 固定 seed 回归；
2. 1:1 场地复刻，≥30 轮随机摆筒全流程；
3. 抗干扰双机同飞演练；故障处置手册（断流/丢检/丢星均有确定动作）；
4. 时间分解表优化（转场 4m/s、侦察 3m/s，目标 T ≤150s）；
5. **验收 M5**：30 轮零终止零零分，平均 ≥1000；每种注入故障均有记录与处置。

### P6 能赛（W9 + §9 → M6）

1. 合规自检表逐条签字；图纸包备查；报名定稿；
2. 上场流程演练（§9.1/9.2 全项，含黑帧预热、紧固、航向核对、preflight_only 预检），全程 ≤60s；
3. 设备交还流程卡；
4. **验收 M6**：连续 3 次完整演练无疏漏；抽审材料齐备。

## 14. 依赖与并行建议

- **关键路径**：P0.1 训练 → P0.2 部署 → P2 感知 → P4 状态机 → P5 稳定性 → P6。
- **最早开工**：数据集（本文 §8.4 采集清单）——打印/实拍/标注周期最长；结构与投放机构（P1-1、P3-1）同步开工，轴线同轴要在 P3 标定前解决。
- 视觉算法与机载软件并行：P0.3/P0.4（黑帧预热、时间戳插值）不依赖硬件到位。
- 飞控调参（P1-3/4）与感知无依赖，可全并行；SITL（P5-1）建议提前到 P4 期间就搭好，让"先仿真后真机"铁律从头生效。
