# 2026-09-13 仓库收稿：两仓 commit（类别表勘正 + 真判读节点 + systemd + M3 仿真线）

> 一句话：把 09-12 深夜三线（视觉勘正与训练定案 / 真判读节点+P0.2/P0.3 上机 /
> 并发仿真线 M3 验收）的全部改动分两仓提交；提交前清掉两处混入的 Windows 构建
> 产物。**未推送远程**，时机由用户定。

## 一、提交清单

| 仓库 | commit | 体量 | 内容 |
|---|---|---|---|
| 根仓库 | `2060fc1` | 68 文件，+8596/−75 | 类别表二次勘正（SSOT §4.4 附件11 实表 + build_real_data.py + 训练指南/脚本停用横幅）；训练路线定案（COCO 直训，deliver_v2 权威，P0.1~P0.3 状态回填）；09-10/11/12 三天日志；`12_使用说明书` 8 模块文档体系首次入库；仿真环境全套脚本；check_radio.py；V6X 参数基线回写；.gitignore 仿真 models/ 规则 |
| 内仓库 `10_机载代码` | `fee631b` | 41 文件，+6005/−96 | hazard_recon_node 真判读节点（params/launch/setup 注册）；systemd 三件套（service/启动脚本/UDP profile）；契约 v1.2 + ReconMarker 类别表勘正；`cuadc_interfaces` 包首次入库；并发线 M3 仿真判分闭环（route_logic 66 单测 / judge 真值参数化 / 场景真值感知替身 / fc_sitl_m3）；deploy_trt_jetson.sh 清华镜像；recon_viewer/fake_recon 类别表勘正 |

## 二、提交前清理（"只入库文本资产"原则执法）

1. `build_test/`（hello.exe/pdb 等编译试验产物，5.7MB）→ 加入内仓库 .gitignore；
2. `cuadc_mission/test/` 里 4 个 `test_*.exe/.pdb` 已混入暂存 → `git rm --cached`
   移出 + 新增 `test_*.exe / *.pdb` ignore 规则（amend 进同一 commit，未推送故安全）；
3. 大小复查：两仓 commit 均无 >500KB 文件；`Frame ZD550.stl` <500KB 保留——它是
   `make_zd550_meshes.py` 再生成 mesh 的 CAD 源，属必要资产；
4. 权重/数据集按白名单留在库外：`deliver_v2/`、`02_数据与训练/hazard_hgd_10cls_v2/`
   （物理备份已在，见 09-12 会话七）。

## 三、状态与下一步

- 两仓工作区干净（main 分支，本地未推送）；
- 下一步承接 09-12 会话九第六节清单：真相机驱动接入 `/camera/image_raw`、
  比赛机 sysctl UDP 缓冲、M4 mission RECON 段与真判读联调、v3 补数据；
  另加并发线遗留：M2 视觉在环桶检测半边（白筒 seg 模型）、safety_monitor。

---

# 会话二（追加）：真相机上机日——相机驱动 30fps 打通 + 判读生产切换 + 操作卡定稿

> 一句话：BL-500W-335 首次上机，全链（相机→ROS2→TensorRT 判读→证据落盘→地面判读表）
> 真图打通并切生产（systemd 压缩图模式）；挖出三个大坑（手动曝光固件无视、raw 图
> 1Hz、QoS 失配）；新资产 camera_node / camera_view / detect_view 三脚本 +
> hazard_recon 压缩图开关；视觉线"能看"正式从占位流升级为真相机流。

## 一、硬件验收（§1.2 基线的上机核对）

- 相机枚举：`/dev/video0` = BL-500W-335（UVC 名 "HDR CAMERA-A"，Realtek 0bda:5180 桥），
  `/dev/video1` 是同相机的元数据节点（不用）；格式表与 11_设备参数 §8 逐项一致
  （MJPG 最高 2592×1944@30，YUYV 仅 640×480@30）；
- 控制项齐全：亮度/增益/白平衡/50Hz 抗闪可设；**自动曝光/自动白平衡可关**成立；
- ⚠️ **手动曝光不受控（飞行红线，未解）**：`exposure_time_absolute` 写入被接受但
  固件无视（20→2047 亮度均值纹丝不动），无厂商扩展单元可绕。§1.2 "锁快门
  ≤1/500s" 当前无法执行——飞行前必须解决（换固件工具路径 / 换相机，或实证户外
  日光下 AE 短曝光足够稳后豁免）。已回写 01 视觉模块 §1.2 警示框。

## 二、三个大坑（全部实测定位，已回写 06 坑清单 §5）

1. **raw Image 6MB/帧 = 全链 1Hz（本日最大坑）**：1080p BGR 裸图在 rclpy+FastDDS
   下单帧序列化/发送 ~1s；RELIABLE 还会被慢订阅端流控堵死。定点实验：同环路发
   1 字节假数据 = 30fps → 锁定发布管线。**解法定案：发 CompressedImage，且直接
   透传相机原生 MJPG（CONVERT_RGB=0）——零编码零二次压缩，~65KB/帧全链 30fps**
   （JPEG 重编码版只有 17Hz，弃）。→ 01 视觉模块新 §4.5 图像传输定案；
2. **QoS 失配 = 一帧都收不到**：发布 best_effort 后，订阅端 RELIABLE（原默认
   `image_qos_reliable: true`）与之不兼容。生产 yaml 已补
   `image_compressed: true` + `image_qos_reliable: false` 两行（src+install 双份）；
3. **v4l2 控制必须开流前写**：流激活中写 set-ctrl 掉 ~1fps 怪模式；且控制值跨
   会话持久、设备重开会回默认。camera_node 固化"先写控制再 VideoCapture +
   每次 open() 后重设"顺序。另：暗屋 AE 拉秒级长曝光同样掉到 1fps——bench 口径
   = 手动模式 + `brightness=48` + 开灯拍摄。

## 三、新资产（内仓库 `10_机载代码/scripts/`，待提交）

| 文件 | 作用 |
|---|---|
| `camera_node.py` | UVC 相机驱动：开流前 v4l2 控制 → 原生 MJPG 透传 `/camera/image_raw/compressed`（CompressedImage，best_effort，stamp=取帧时刻符合契约 §5.3），断流自重连 |
| `camera_view.py` | 纯画面查看器（机身屏幕，按 q 退） |
| `detect_view.py` | **识别框查看器**：旁路跑 engine 画框，框色=门控语义（绿=过门控/橙=灰区竞争/红=拒识），标签=类别号+拼音（cv2.putText 不支持中文，中文会渲染成 ????） |
| `hazard_recon_node.py`（改） | 新增 `image_compressed` 参数（默认 false 向后兼容）：订阅 `<topic>/compressed`，`cv2.imdecode` 解码后走原管线 |

生产切换已执行：colcon build → yaml 补两行 → systemd 重启 → 引擎 0.38s+预热就绪。

## 四、端到端验证时间线（生产口径，RECON 状态门控开）

| seq | 场景 | 判读结果 | 判读 |
|---|---|---|---|
| 10 | 空场景 | 无结论 [RECON_SURVEY] | ✅ 链路+状态门控通 |
| 11 | 镜头对桌面 | 无结论 | ✅ 拒识正确 |
| 12 | 爆炸品贴纸特写 | `#0:留空(不足) conf0.85 1帧` | ✅ 门控拒了不可靠检测 |
| 13/14 | （服务被停） | 无响应 | 发现服务 inactive（人为 stop，非崩溃） |

seq=12 根因（证据帧离线推理定位）：**桌面特写严重偏离训练分布**——模型在 4m 俯拍
数据上训练，对占画面 1/3 的倾斜大贴纸完全不出检（原始检测全是鼠标/键盘上的低分
误检）。操作卡 §④ 摆靶要点由此而来：模拟俯拍几何（镜头朝下、标识平放地面、
0.5~1.5m、开灯、画面中央）。判读链逻辑（窗口/融合/拒识/证据 JSONL/裁剪图）全部
行为正确。

## 五、文档回写清单

| 文档 | 改动 |
|---|---|
| `01_视觉模块.md` | §1.2 手动曝光警示框；§4.5 图像传输定案表；§6.1 桌面真机判读验证操作卡（9 步，命令全实测）；§6.2 日常运行速查；§8 待办 4 勾注 |
| `06_坑清单与排障.md` | §5 新增 5 行：手动曝光不受控 / 暗屋 AE 1fps / 流中写控制 1fps / raw 图 1Hz→压缩透传 / pkill 自匹配（再次咬人） |
| `12_使用说明书/README.md` | "按问题找文档"加 3 行（图像传输→01 §4.5，运行速查→01 §6.2，摆靶验证→01 §6.1） |
| SSOT `01_设计/总体方案与执行计划.md` | §10.1 明确不做 +1 行：K230/CanMV+二维云台评估否决（上午定案） |

## 六、GitHub 收稿（当日第三件）

两仓全部推送至 [github.com/Tracysrs/CUADC](https://github.com/Tracysrs/CUADC)（私有）：

| 分支 | commit | 内容 |
|---|---|---|
| `main`（内仓库镜像，原有） | `4b12cf4` | 相机三脚本 + hazard_recon 压缩图开关 + config 生产双行 + 09-12 排障两件（SEARCH 沉降期 / 白桶相对阈值 l_offset+直径补偿 1.15），含积压的 `fee631b` |
| `project-main`（**新建分支**） | `62be1e8` | 根仓库全宗：视觉文档三件 + 坑清单 5 条 + 本日志会话二 + SSOT K230 否决行 + 仿真排障探针（mavlink_sniffer/perception_probe/fc_sitl_m2 强清残留） |

**结构定案**：根仓库与内仓库是两套独立 git 历史，不能共用 main——`main` 保持
机载代码不动，根仓库整库推 `project-main`。以后根仓库更新 = `git push origin main:project-main`。
顺手修一隐患：仓库内 `hazard_recon_params.yaml` 此前没带生产切换两行（只在 Jetson
上改了），已同步入库，防换机部署时生产配置退回 raw 图模式。

## 七、待办（更新后全景）

1. **飞行红线**：手动曝光不受控——豁免论证或换件，需拍板；
2. **M4 填单最后一公里**：判读表→MAVLink STATUSTEXT 数传桥（433 空口 57.6k 只跑得
   动文字），recon_viewer 目前只能本机看；
3. 俯拍几何摆靶复测 seq≥15（预期融合 ≥5 帧、出真判读；桌面特写检不出=分布外已定论）；
4. 比赛机 sysctl UDP 缓冲、M4 RECON 段联调、M2 白筒 seg 半边、safety_monitor（承接 09-12 清单）；
5. ~~本会话内仓改动待 commit~~ ✅ 已随 `4b12cf4`/`62be1e8` 收稿推送（见 §六）。
---

# 会话三（追加）：M2 视觉在环（CV 真感知）——链路全通、锁定实证一次，终验收挂起

> 承接 09-12 收稿清单第 1 项。当日战果：**渲染世界 + gz-transport Python 直连 +
> CV 白桶真感知节点全链打通，真图像检测曾完成一次 3 筒锁定**（M2B）；随后四轮
> 验收被环境因素连环打断（渲染光照漂移 / 残留飞行控制冲突 / 热点切换断连），
> CV 通道本身的最终调参挂起。深夜 Jetson 离线，本会话收稿。

## 一、方案与资产（全部已入库/部署）

- **桥接定案**：Jetson 无 ros_gz 包（apt 无 humble-ros-gzharmonic）→ 改用
  **gz-transport13 Python 绑定直订 `/d435i/image`**，零安装绕开桥接坑。三个
  使用要点：必须 `PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python`；subscribe
  签名 `(msg_type, topic, callback)`；rgbd 相机像素格式 int==3 才是 RGB8。
- **渲染世界**：飞行世界加入 Sensors 系统（ogre2），相机 30→10Hz、848×480→
  640×360（实测 RTF 0.49 与渲染无关，是 Jetson 基线——历史 5 轮 M3 本就在此
  RTF 下跑，无需放宽任务超时）。
- **CV 节点** `bucket_cv_perception_node.py`（已注册 colcon）：LAB 白桶分割 +
  fitEllipse + 单目解算 → 契约 PoseArray（stamp=最新 odom 戳−延迟、直径补偿、
  哨兵、空帧照发），与真值替身同契约，mission 零改动切换。
- **验收脚本** `fc_sitl_m2.sh`：[0] 强清残留（僵尸任务节点会共享 mavros 发
  disarm 把新任务在空中打下来——STATUSTEXT 铁证）+ 与 fc_sitl_m3 同构七步。
- **排障工具**：`stamp_probe.py`（双话题对戳）、`img_dump.py`（gz 相机抓帧）、
  `perception_probe.py`（感知话题全量录制）、`mavlink_sniffer.py`（5763 抓
  CMD/STATUSTEXT 定责 disarm 来源）。

## 二、实证战绩

1. **契约链路通 + 真图像锁定一次**（M2B 诊断轮）：任务侧仅 1 帧启动期拒收，
   "目标集冻结(normal, 3 筒)"由真图像检测达成——gz 相机→CV→契约→BucketMap→
   锁定全链为真。
2. **标定悬停**（mavros 服务手飞）：轴向全对（x +0.56 vs 期望 +0.57 / y −0.71
   vs −0.61），直径偏小 ~15% → `diam_compensation=1.15` 补偿。先验校验白捡
   一功：起飞坪白盘（13 万像素）与白色场地标线（圆度 0.14 / 反算直径 1.0m）
   全被直径/圆度门正确拒掉。
3. **SEARCH 沉降期**（search_settle_s=8）：起飞后 EKF 飞行中罗盘对齐期间喂图
   会凑出假航迹锁错目标（M2B 首目标 (25.5,-4.7) ≠ 桶位的根因）；真值替身误差
   自相消免疫此问题，真视觉一上来就暴露——已修入 mission_node + yaml。

## 三、卡点与环境教训（终验收挂起的原因）

1. **渲染光照漂移**：Jetson 重启后 ogre2 回退软渲染（libEGL nvidia-drm 告警），
   场景亮度被压平（全帧 L 挤进 [97,159]），白桶对场地对比度 ≤2 个灰度——固定
   阈值差 1 全灭，改相对阈值（帧中位数+偏移）也受制于"中位数≈最大值"的极端
   情况。世界文件 `scene.ambient` 1.0→0.4 后对比度恢复正常（白桶 L230 vs 场地
   145），但随后抓帧分析显示检测门限仍对光照/角度敏感——经典 CV 对渲染条件
   的鲁棒性有限。
2. **并发控制冲突**：本机同夜在跑真相机线（camera_node 三实例 + hazard_recon，
   见会话二），仿真飞机多次被残留指令留在 GUIDED 悬停（FCU 保持最后目标点
   特性），导致 M2K 启动即"飞机已武装"安全失败；已发紧急 disarm 收场。
   **教训：仿真飞行实验与真机线共用 Jetson 时必须先 `pgrep -af _node` 清场，
   确认 FCU disarmed 再开跑。**
3. 网络：热点切换导致 IP 变更（10.66.24.217 → **10.234.165.217**，SSH config
   已更新）+ 断连多次杀后台链路（setsid/nohup 双保险已成熟应对）。

## 四、工程判断（写在这里供拍板）

经典 CV 通道（LAB+椭圆）已把链路打通并完成一次真实锁定，但要在渲染光照漂移下
做到任务级鲁棒，调参收益递减。**建议把 M2 视觉的完成态押在主通道：白桶 seg
模型训练**（SSOT §4.1 原设计，数据可从渲染世界离线批量采集——现在相机链路已
通，采图脚本成本极低），CV 节点保留作副通道兜底（SSOT 原设计即双通道）。这
与真机线路径一致（真相机 30fps 已通，hazard_recon 生产化完成，同一套工程方法
复制到白桶即可）。

## 五、下一步

1. 白桶 seg 数据采集（渲染世界批量采图+标注流水线）→ 训练 → 替换/叠加主通道；
2. M2 终验收（需独占飞机时段）：reset → fcu_ready → fc_sitl_m2.sh，验收线
   "3 筒冻结贴桶位 + 投放 2/2 A 区"；
3. 飞行红线（承接会话二）：手动曝光不受控需拍板。

---

# 会话四（补记）：三端对齐 + project-main 推送

> 一句话：本地仓库 / Jetson 部署 / git 历史三层核对一致；根仓库 `8132396` 推送
> 至 project-main（`96e8981..8132396`）。

## 一、三层对齐结果

1. **文件级（本地 ↔ Jetson，md5 16/16 一致）**：机载代码 9 件（mission_node /
   mission_params / route_logic / test_route_logic / CMakeLists / package.xml /
   setup.py / bucket_cv 节点 / 真值替身）+ 仿真脚本 5 件（fc_sitl_m2/m3 /
   start_sitl / 飞行世界 / mavlink_sniffer）+ 模型与世界 2 件（相机 10Hz/640×360
   / 渲染世界）；
2. **反向核对**：hazard_recon_params.yaml 三处（本地仓 = Jetson src = install
   已装配置）逐字一致——生产切换无"只在 Jetson 改了"漂移；
3. **Git**：内仓 `10_机载代码` 干净（当日 M2 代码已随 `4b12cf4` 收走并推送）；
   根仓库补 commit `8132396`（场景 ambient 1.0→0.4 光照修复 + 本日志会话三 +
   README 索引），随即推送。

## 二、热点切换备忘

Jetson IP 已从 10.66.24.217 变更为 **10.234.165.217**（换热点 DHCP 重配），
`~/.ssh/config` 的 `jetson` 别名已更新（host key 重新 accept）。下次连不上先
ping 新 IP / 扫网段（python 并发 connect 22 端口脚本 10 秒定位）。

## 三、状态

- 两仓工作区干净；根仓库 main 与 project-main 同步至 `8132396`；
- 下次上机待办不变（承接会话三 §五）：M2 终验收（独占飞机时段）+ 白桶 seg
  采图训练 + 手动曝光红线拍板。

# 会话五（追加）：仿真任务操作手册落地

- 新增 `03_工具/仿真环境/jetson/操作手册.md`：六步标准流程（reset→门禁→GUI→
  任务→看结果→兜底）、三种任务模式对照表（M1/M3/M2）、判分验收线、排障速查表
  （10 条症状→原因→动作，全部源自实测）、IP 变更处理、文件同步三落点与红线；
- 仿真环境 README 的 Jetson SITL 节顶部加手册直达链接（本节转历史留存）；
- 同步至 05_机载系统与仿真模块的手册地图由下次文档巡检顺带（本手册已放在脚本
  同目录，按"就近"原则可发现）。

# 会话六（追加）：随机布景 + 完成事项输出（应用户需求）

- **`randomize_scene.py`（新）**：任务区内随机摆放 3 投放桶（间距≥1.5m）+ 5 侦察桶
  + 3 张危化品标识（10 类纹理池，随机 3 桶），三处真值一次联动——飞行世界场景段
  重写 / generated_scene.yaml 真值 / judge 判分参数；`--restart-gz`（默认开）只重启
  gz 载入新场景，SITL 不动 → **无 15~20min 不稳定窗**（实测门禁 1~3 分钟即过）。
- **fc_sitl_m3/m2.sh**：judge 启动挂 `judge_truth_params.yaml`（布景联动）+ 结尾
  输出结构化**"完成事项"**（状态链耗时 / 投放明细误差与分区 / 拍照确认 / 总时长 /
  判分累计；含失败原因解析）。
- **实跑验证（seed 20913）**：新桶位 (28.80,-0.01)(31.51,1.45)(27.96,-3.59) 全随机，
  任务首轮即**锁定 3 筒跟随新布局、投放 2/2 全 A 区（4.7cm/5.5cm）、判分 400**，
  judge 与真值替身全部吃到新坐标——随机布景闭环成立。完成事项格式两处小 bug
  （时间戳多除 1e9 显示 0s；误差 m 误标 cm）当场修复。
- 用户自跑命令：`randomize_scene.py → fcu_ready.py → fc_sitl_m3.sh`（见操作手册 §0）。

# 会话七（追加）：fcu_ready v2——双端口 + GPS 校验 + 自愈（应用户"连接稳定快速"）

- **问题**：下午用户手动跑门禁 60s 全 refused。排查发现两个独立问题：(a) 5762
  监听在多次连接后会消失（SITL TCP serial 特性，原因未深究）；(b) 更隐蔽——
  长时间空转后 gz→SITL 的 **GPS 耦合僵死**（心跳/姿态照常流、GPS 卡 0），旧门禁
  只探心跳流会误放行，任务将卡死 WAIT_NAV_STABLE。
- **fcu_ready v2 三层判据**：①双端口轮询（5762 优先，5760 空闲即用——手动跑门禁
  时 mavros 未起、直连最快；任务脚本内 5760 被占自动跳过）；②心跳稳定窗（同 v1）；
  ③**GPS fix≥3D 才算真就绪**。
- **自动自愈**（默认开）：GPS 卡 0 超 60s 且 SITL 在跑 → 自动重启 gz 服务端
  （FDM 重握手，SITL 不动）继续探，最多 4 次——把"玄学等 20 分钟"变成 ~1 分钟。
- **实测**：僵死状态下新版门禁 15:37 当场通过（20s 窗 + GPS 3D ✓）。
  任务脚本无需改动（门禁参数向后兼容）。

操作手册同步（会话六续）：§0 速查、§1 ② 门禁整节重写（v2 三层判据+自愈说明）、
§5 排障表三行更新（mode:'' 空转 / 门禁 refused / GPS 卡 0 均指向 v2 行为）。

---

# 今日总结（收稿）

> 一句话：仿真任务线从"能跑"进化到"好用"——M2 CV 视觉链路打通并完成一次真图像
> 3 筒锁定，随机布景+完成事项让任务可反复独立验证，门禁 v2 把玄学等待变成 1 分钟
> 自愈；全套操作沉淀为一份操作手册。当前版本 = M1 能飞 ✅ / M2 感知契约+CV 链路 ✅
> （终验收待独占时段）/ M3 能投判分 ✅（7 轮 14 投全 A 区）。

## 完成主线（时间序）

1. **会话一（两仓收稿）**：类别表勘正 / 真判读节点 / systemd / M3 判分闭环入库，
   提交前清两处 Windows 构建产物；
2. **会话二（真相机上机日）**：BL-500W-335 全链真图 30fps 切生产；三大坑
   （手动曝光无视=飞行红线 / raw 图 1Hz→MJPG 透传 / QoS 失配）；GitHub 首推
   （main=机载代码、根仓库全宗=project-main，结构定案）；
3. **会话三（M2 CV 视觉在环）**：飞行世界加渲染 + gz-transport13 Python 直订
   （无 ros_gz 也不装）+ bucket_cv 真感知节点（契约 PoseArray）；**真图像 3 筒
   锁定实证**；排障六件（odom 戳同源 / 锁定新鲜窗 60s / ALIGN 容忍窗 1+4s /
   直径补偿 1.15 / SEARCH 沉降期 8s / 相对阈值抗光照）；经典 CV 对渲染光照
   鲁棒性有限 → 工程建议=白桶 seg 训练为主通道；
4. **会话四（三端对齐）**：md5 16/16 全一致 + hazard 配置三处一致 + project-main
   推送；热点切换 IP 变更处理流程沉淀；
5. **会话五（操作手册）**：六步标准流程 / 三模式对照 / 验收线 / 排障速查 10 条 /
   红线，落在脚本同目录；
6. **会话六（随机布景+完成事项）**：randomize_scene.py 三真值联动（世界/真值/
   judge）+ gz-only 重启免长等待；fc_sitl_m3/m2 输出结构化完成事项；
   seed20913 首轮 2/2 全 A 区（4.7/5.5cm）；
7. **会话七（fcu_ready v2）**：双端口探活 + GPS 3D 校验 + 耦合僵死自动重启 gz
   自愈；僵死状态实测当场通过，任务脚本零改动。

## 关键数字

- M3 判分闭环：**7 轮任务 14 次投放全部 A 区**（误差 3.8~7.6cm，含两套随机布局），
  全程 125~142s（150s 冲奖线内），零失败零弃桶
- 门禁 v2：僵死状态 ~1min 自愈就绪（旧版玄学等 5~20min 且会误放行）
- 单测 66+145 全绿；三端对齐 md5 16/16；两仓+GitHub 全同步

## 下一步清单（全景，按优先级）

1. **M2 终验收**（需独占飞机时段）：reset → 门禁 → fc_sitl_m2.sh，验收线
   "3 筒冻结贴桶位 + 投放 2/2 A 区"；
2. **白桶 seg 模型训练**（M2 完成态主通道）：渲染世界批量采图（相机链路已通，
   采图脚本成本极低）→ 标注 → 训练 → 替换/叠加主通道，CV 留副通道兜底；
3. **飞行红线拍板**：真相机手动曝光不受控——豁免论证或换件（会话二遗留）；
4. **M4**：hazard_recon classification 接进任务主链 + safety_monitor 独立进程；
5. **真机线**：装桨 → 真机 M1 首飞（拆桨预检）→ 场地几何实测后修订
   mission_params 场地组 → NE0-3/RTK 调试收尾。

## 九、会话八：任务模式统一入口 fc_task.sh

用户问"选任务模式的启动脚本是什么"——三个脚本即三个模式（模式开关=任务节点的
`m1_no_vision_mode`/`sim_release_bridge`，脚本已预设）。为免记名，新增统一入口
`fc_task.sh [mission|m3|m2] [秒数]`（exec 转发，缺省 m3），操作手册 §0/§2 同步。

说明书同步（会话八续）：03_操作模块 §5.1 重写为"四步标准流程+模式表"（randomize/
reset/GUI/门禁 v2/fc_task）、§5.2 步骤更新（[0] 强清残留 + 门禁 v2）；05_系统模块
§5.5 加 fc_task 入口说明 + fcu_ready 门禁 v2 全段重写。三处文档（jetson 操作手册/
03 操作模块/05 系统模块）与脚本行为一致。

# 会话九（追加）：视觉模块全链落地——桶感知真机节点 + H 圆精准降落链 + 设计文档定稿

用户指令"处理视觉模块，根据任务详情详细规划视觉算法设计，先写计划再执行"。计划
模式调研（3 个探索代理 + SSOT/契约/算法模块/视觉模块四份文档精读）→ 计划批准
（范围选"全链落地"）→ 执行完成。

## 一、关键发现（写进历史的三件）

1. **PLND_TYPE 文档矛盾实锤**：04_算法模块 §11 旧文"PLND_TYPE=0（0=CompanionComputer；
   1=IR-LOCK）"把含义写反——ArduPilot 实际 0=Never / **1=CompanionComputer MAVLink** /
   2=IRLOCK，SSOT §4.5 的 PLND_TYPE=1 才对。已勘误 04_算法模块 §11 并在契约/设计
   文档按勘误后口径书写；
2. **"MAV_CMD_NAV_LAND_TARGET"疑为笔误**（MAVLink 无此命令）：主链路定案 =
   `/mavros/cmd/land` → LAND 模式 → PLND 检测到有效目标自动接管；mission_node
   LAND 态零改动。待上机对照 4.7 固件文档后回填；
3. **SSOT §4.2 单目测距公式 Z=f·H/h_px 在正下视中心区域无定义**（看不到筒侧壁）
   ——设计定案主用 odom 相对高度解算（仿真已验证路径），原公式降级为边缘视角
   sanity。04_算法模块 §2 已加口径修正注。

## 二、代码落地（产出区/10_机载代码/，本机单测全绿）

- **`cuadc_perception/vision_core.py`**：纯算法库（无 ROS，Windows/Jetson 共用）——
  内参（calib yaml + datasheet FOV 兜底 + 正交性校验阻断启动）、单目解算（odom 高度
  主用 + mount_rot_deg 装订旋转参数化）、LAB 白桶分割（仿真标定值直迁）、Hough H 圆
  （动态半径窗 + H 图案验证打分：Otsu 少数派 + 左右竖带对称性 × 存在性门）、双通道
  IoU 融合、后处理四件（同帧去重/独立性/直径先验对号/中位平滑）、MedianSmoother、
  FLU→FRD、SHA-256；
- **`bucket_perception_node.py`（M2 缺口补齐）**：真机版白桶感知——compressed 订阅
  + odom 插值取帧高度（P0.4 同款容差）+ 双通道 + 后处理四件 → 契约 v1.3 PoseArray
  + 心跳；**分级上线**：bucket.engine 缺失/SHA 不符自动降级 LAB-only（数据缺口不
  阻塞真机链路）；active_states 状态门控省 CPU（非激活态照发空检测+心跳）；
- **`h_circle_node.py`（M4 提前）**：H 圆 → 质量门控（直径-高度一致性 0.80±0.25m +
  连续 3 帧确认）→ `/mavros/landing_target/set`（BODY_FRD，FLU→FRD y/z 取反单点
  收口；angle/distance/position/size 全填，字段 hasattr 防御 MAVROS 版本差异）+
  heartbeat_land；backend=yolo|hough|both，engine 缺失自动 Hough 兜底；
- **`scripts/calibrate_camera.py`**：棋盘格标定（RMS/最差单图/fy-fx 比自检 + datasheet
  交叉核对）+ 已知尺寸快检模式；冒烟验证：合成靶 fx 反算偏差 0.5%；
- **`deploy_trt_jetson.sh` 多模型化**：slot = recon|bucket|hcircle 三槽位，engine 入
  `~/cuadc_models/<槽位>.engine` + SHA256SUMS.txt 台账 + 提示回填 expected_sha256；
- setup.py/package.xml 登记新节点与 sensor/nav/mavros/builtin 依赖；test 包补
  `__init__.py`（unittest discover 可用）。

## 三、验证（本机）

- `test_vision_core.py` **48 例全绿**（内参/解算/四件/融合/平滑/LAB/Hough/FLU-FRD/
  JPEG 往返/常量契约一致），mission 侧 39 例回归不受影响；
- 渲染器 × LAB 闭环：合成帧真值 22 筒 LAB 副通道 22 检出（100%）；
- build_barrel_dataset 冒烟：40 张合成 → train 38/val 2 + data.yaml + 版本记录；
- 修了三个自测抓到的 bug：内参缩放先于校验（分辨率不一致时 cx 越界误报）、渲染器
  广播 bug、合成文件名片段号导致全进单 split。

## 四、数据与训练工具链（02_数据与训练/barrel_seg_tools/，采集即用）

- `render_barrel_synthetic.py`：**v1 合成集只有检测框、与 seg 不兼容**（文档化了）——
  新渲染器直接出 YOLO-seg 多边形掩码（合成掩码免费），复刻比赛成像几何（蓝地/水泥
  地、三档筒径、1.5~4.5m 高度、斜视椭圆、阴影、光照梯度、亮白干扰物）；
- `build_barrel_dataset.py`：合成+实拍合并、按片段防泄漏切分、空标签背景帧、坏标
  即退、dataset_version.md 定式；
- `run_barrel_training.sh` / `run_hmarker_training.sh`：AutoDL 一键（铁律沿用：
  flipud=0.5 / hsv_h=0.005 / close_mosaic=10，deliver 包 + SHA256SUMS）；
- `采集操作卡_白筒与H圆.md`：拍摄矩阵（高度/角度/布景/光照/干扰）、标注口径
  （多边形贴筒口外沿、片段号命名）、**白底黑 H 制式定案**（h_pattern_score 假设
  此前提）、危化 v3 顺路清单、建集前自检表。

## 五、文档（六处）

1. **新增《01_设计/视觉算法设计.md》**：三任务全链详细设计（一页总览/任务规格/
   架构/公共基座/桶感知/H 圆与 PLND/标定流程/数据训练/上机风险清单 7 项/验收门
   8 项/自查题 6 题），SSOT 文档地图已收录；
2. 04_算法模块：§11 PLND_TYPE 勘误 + §2 解算口径修正注；
3. 01_视觉模块：§0 全景表（三消费者 + 模型状态）、契约版本引用 v1.3、§8 待办重排；
4. 接口契约 **v1.3**：新增 §6 LANDING_TARGET 流 + heartbeat_land + h_circle_body
   调试话题（原 §6/§7 顺延）；桶契约字段/哨兵/QoS 不变，校验器机器可查子集不变；
5. 10_机载代码 README：感知包条目与里程碑刷新；
6. 本日志。

## 六、下一步（接续全景）

1. **上机三连**：两节点 colcon build + `check_vision_contract.py` 对真机桶节点全项
   PASS + perception_probe 录检测质量（LAB-only 态）；
2. **外场采集**：按采集卡拍白筒 ≥600 张 + H 圆 ~200 张（顺路危化 v3）→ 建集 →
   AutoDL 训练 → deploy bucket/hcircle 槽位 → 回填 SHA → 双通道升级；
3. **内参标定 + 悬停偏置目标实验**（定 mount_rot_deg，两个节点共用）；
4. PLND 飞控侧：verify_params 核 PLND_TYPE=1 + landing_target 插件在 apm.launch 的
   加载确认 + MAVROS LandingTarget 字段核对（设计文档 §8 风险 1/2）；
5. M2 终验收（会话三遗留）与 M4 safety_monitor 继续排队。

## 七、环境注意（本次实测）

- 会话中途工作目录被并行重组（三分区：产出区/记录区/查阅区，commit 00f7463 +
  遗留修正 8af283b）——本会话全部产出按新路径落位，与重组提交无交叉；重组作者
  另有 ops.py 未提交改动与 servo_test.py 新文件，不在本会话提交范围。
