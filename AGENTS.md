# AGENTS.md — AI 协作规则手册

> 受众是 AI 会话（下次接手的自己 / 其他 agent）。人类新人入口见 [README.md](README.md)；
> 本文件只放「不看到就会犯错」的规则与指针，不写历史叙事，过程细节去 `07_工作日志/` 查。

## 硬规则（红线）

1. **方案 SSOT 唯一**：改方案只改根目录 `总体方案与执行计划.md`（其 §0 是全仓文档地图）。其余文档一律不改写方案内容，只引用；说明书与 SSOT 冲突时以 SSOT + 最新日志为准。
2. **工作日志单文档制**（2026-09-19 起）：唯一日志 = `07_工作日志/工作日志总编.md`——新会话在**文末**追加一节 `## 2026-MM-DD 主题`（绝对日期，禁相对时间词），并同步总编顶部目录。错误/教训沉淀到 `06_使用说明书/11_错误与经验总结.md`。按日历史原文在 `99_归档/工作日志按日版_20260919/`（只读）。
3. **只读区**：`08_参考资料/`（第三方版权件与他人仓库，不入库）与 `99_归档/`（过时材料，不入库）内容一律不改；参考仓结论必须落入 SSOT/设计文档才生效。
4. **白名单入库**：`.gitignore` 为「默认全忽略 + 白名单放行」。数据集、训练产物（权重/图表）、Gazebo models、dataflash `.BIN` 一律本地保留不入库；新增放行项前先确认体量与版权边界，严禁顺手放宽。
5. **结构变更双同步**：重命名/移动任何目录，必须同步更新根 README 目录总览 + SSOT §0 文档地图 + 相关模块 README；文件迁移用 `git mv` 保 rename 历史。
6. **飞控与接线安全**：连飞控前先断开 Mission Planner（占 COM 口）；参数导入必须两遍（中间重启），写参数后等 5 秒再重启；RTK-01-R 接线 **FC k 针 → rover k+1 针循环移位，严禁 1:1 直通**（5V 撞 GND）；电机/舵机测试拆桨进行。

## 布局速查

| 路径 | 内容 |
|---|---|
| `总体方案与执行计划.md`（根） | SSOT：方案 + 执行计划 + §0 文档地图 |
| `01_视觉感知/` | 视觉算法设计 + 数据与训练（指南/脚本/白筒采集工具/版本台账） |
| `02_飞控与硬件/` | V6X 参数与参数备份、固件存档、调试工具（`ops.py` 统一入口）、设备参数、飞行日志（回拉+台账，.BIN 不入库） |
| `03_机载软件/` | ROS 2 三包 + `接口契约.md` + `时间同步设计.md`（2026-09-17 起的路径，Jetson 侧同名工作目录需对应更新） |
| `04_仿真/` | SIH 台架三件套（计划/操作/参数）+ Gazebo 仿真环境 |
| `05_操作与比赛/` | 上场操作卡两份（比赛日打印携带） |
| `06_使用说明书/` | 成套说明书 12 卷：01~08 执行手册（按模块步骤）+ 09/10 原理入门卷 + 11 错误与经验总编（入口 README，新人路线 10→00） |
| `07_工作日志/` | 唯一《工作日志总编》（**进展权威入口**，以最新小节为准） |

## 深入文档指针

| 要做什么 | 去哪 |
|---|---|
| 方案取舍依据 / 改方案 | SSOT（根目录）；挂起的方案决策看总编「待拍板清单」 |
| 各模块怎么用 / 怎么复现 | `06_使用说明书/README.md` 模块地图 |
| 连飞控 / 调试脚本用法 | `02_飞控与硬件/调试工具/README.md`；日志回拉入 `02_飞控与硬件/飞行日志/` |
| 代码编译 / SITL 联跑 / M1 验收 | `03_机载软件/README.md` |
| SIH 台架操作 | `04_仿真/SIH台架/SIH台架操作.md` |
| 某个结论当初怎么来的 | `07_工作日志/工作日志总编.md` 对应日期小节，命令级细节查 `99_归档/工作日志按日版_20260919/` |

## 提交惯例

提交信息中文单段详述（改了什么 + 为什么 + 关键 pin/哈希）；目录迁移一律 `git mv`；
涉及路径变更时在提交信息里写明对 Jetson 侧的影响（cd 路径 / 脚本别名 / systemd WorkingDirectory）。
